/* Generated by Gabbro -- https://github.com/SimonVitzethum/Gabbro
 *
 * This file was generated. Edit the .gab source, not this file.
 *
 * Gabbro is AGPL-3.0 with an additional permission: this generated file and the program it
 * belongs to are NOT a derived work of Gabbro. The permission is conditional on this notice
 * being preserved. See LIZENZ-ZUSATZ.md.
 */
#include <stdint.h>
#include <stdbool.h>
#include <stdatomic.h>
#include <math.h>
/* The shift and conversion this generator relies on are implementation-defined in C11 --
 * so the prelude pins them (PLAN-BITS.md section 5b), on EVERY unit, float or not.
 * Two's complement itself is the language's own rule and needs no probe. */
_Static_assert((-1 >> 1) == -1, "arithmetic right shift");
_Static_assert((int)0xFFFFFFFFu == -1, "modular conversion");

_Atomic uint32_t GESAMT;

_Atomic uint32_t VERWORFEN;

_Atomic uint32_t AKZEPTIERT;

_Atomic uint32_t ABGEWIESEN;

_Atomic uint32_t FEHLFORM;

_Atomic uint32_t VOLL;

_Atomic uint32_t REGELSUMME;

_Atomic uint32_t REGEL000;

_Atomic uint32_t REGEL001;

_Atomic uint32_t REGEL002;

_Atomic uint32_t REGEL003;

_Atomic uint32_t REGEL004;

_Atomic uint32_t REGEL005;

_Atomic uint32_t REGEL006;

_Atomic uint32_t REGEL007;

_Atomic uint32_t REGEL008;

_Atomic uint32_t REGEL009;

_Atomic uint32_t REGEL010;

_Atomic uint32_t REGEL011;

_Atomic uint32_t REGEL012;

_Atomic uint32_t REGEL013;

_Atomic uint32_t REGEL014;

_Atomic uint32_t REGEL015;

_Atomic uint32_t REGEL016;

_Atomic uint32_t REGEL017;

_Atomic uint32_t REGEL018;

_Atomic uint32_t REGEL019;

_Atomic uint32_t REGEL020;

_Atomic uint32_t REGEL021;

_Atomic uint32_t REGEL022;

_Atomic uint32_t REGEL023;

_Atomic uint32_t REGEL024;

_Atomic uint32_t REGEL025;

_Atomic uint32_t REGEL026;

_Atomic uint32_t REGEL027;

_Atomic uint32_t REGEL028;

_Atomic uint32_t REGEL029;

_Atomic uint32_t REGEL030;

_Atomic uint32_t REGEL031;

_Atomic uint32_t REGEL032;

_Atomic uint32_t REGEL033;

_Atomic uint32_t REGEL034;

_Atomic uint32_t REGEL035;

_Atomic uint32_t REGEL036;

_Atomic uint32_t REGEL037;

_Atomic uint32_t REGEL038;

_Atomic uint32_t REGEL039;

_Atomic uint32_t REGEL040;

_Atomic uint32_t REGEL041;

_Atomic uint32_t REGEL042;

_Atomic uint32_t REGEL043;

_Atomic uint32_t REGEL044;

_Atomic uint32_t REGEL045;

_Atomic uint32_t REGEL046;

_Atomic uint32_t REGEL047;

_Atomic uint32_t REGEL048;

_Atomic uint32_t REGEL049;

_Atomic uint32_t REGEL050;

_Atomic uint32_t REGEL051;

_Atomic uint32_t REGEL052;

_Atomic uint32_t REGEL053;

_Atomic uint32_t REGEL054;

_Atomic uint32_t REGEL055;

_Atomic uint32_t REGEL056;

_Atomic uint32_t REGEL057;

_Atomic uint32_t REGEL058;

_Atomic uint32_t REGEL059;

_Atomic uint32_t REGEL060;

_Atomic uint32_t REGEL061;

_Atomic uint32_t REGEL062;

_Atomic uint32_t REGEL063;

_Atomic uint32_t REGEL064;

_Atomic uint32_t REGEL065;

_Atomic uint32_t REGEL066;

_Atomic uint32_t REGEL067;

_Atomic uint32_t REGEL068;

_Atomic uint32_t REGEL069;

_Atomic uint32_t REGEL070;

_Atomic uint32_t REGEL071;

_Atomic uint32_t REGEL072;

_Atomic uint32_t REGEL073;

_Atomic uint32_t REGEL074;

_Atomic uint32_t REGEL075;

_Atomic uint32_t REGEL076;

_Atomic uint32_t REGEL077;

_Atomic uint32_t REGEL078;

_Atomic uint32_t REGEL079;

_Atomic uint32_t REGEL080;

_Atomic uint32_t REGEL081;

_Atomic uint32_t REGEL082;

_Atomic uint32_t REGEL083;

_Atomic uint32_t REGEL084;

_Atomic uint32_t REGEL085;

_Atomic uint32_t REGEL086;

_Atomic uint32_t REGEL087;

_Atomic uint32_t REGEL088;

_Atomic uint32_t REGEL089;

_Atomic uint32_t REGEL090;

_Atomic uint32_t REGEL091;

_Atomic uint32_t REGEL092;

_Atomic uint32_t REGEL093;

_Atomic uint32_t REGEL094;

_Atomic uint32_t REGEL095;

_Atomic uint32_t REGEL096;

_Atomic uint32_t REGEL097;

_Atomic uint32_t REGEL098;

_Atomic uint32_t REGEL099;

_Atomic uint32_t REGEL100;

_Atomic uint32_t REGEL101;

_Atomic uint32_t REGEL102;

_Atomic uint32_t REGEL103;

_Atomic uint32_t REGEL104;

_Atomic uint32_t REGEL105;

_Atomic uint32_t REGEL106;

_Atomic uint32_t REGEL107;

_Atomic uint32_t REGEL108;

_Atomic uint32_t REGEL109;

_Atomic uint32_t REGEL110;

_Atomic uint32_t REGEL111;

_Atomic uint32_t REGEL112;

_Atomic uint32_t REGEL113;

_Atomic uint32_t REGEL114;

_Atomic uint32_t REGEL115;

_Atomic uint32_t REGEL116;

_Atomic uint32_t REGEL117;

_Atomic uint32_t REGEL118;

_Atomic uint32_t REGEL119;

_Atomic uint32_t REGEL120;

_Atomic uint32_t REGEL121;

_Atomic uint32_t REGEL122;

_Atomic uint32_t REGEL123;

_Atomic uint32_t REGEL124;

_Atomic uint32_t REGEL125;

_Atomic uint32_t REGEL126;

_Atomic uint32_t REGEL127;

_Atomic uint32_t REGEL128;

_Atomic uint32_t REGEL129;

_Atomic uint32_t REGEL130;

_Atomic uint32_t REGEL131;

_Atomic uint32_t REGEL132;

_Atomic uint32_t REGEL133;

_Atomic uint32_t REGEL134;

_Atomic uint32_t REGEL135;

_Atomic uint32_t REGEL136;

_Atomic uint32_t REGEL137;

_Atomic uint32_t REGEL138;

_Atomic uint32_t REGEL139;

_Atomic uint32_t REGEL140;

_Atomic uint32_t REGEL141;

_Atomic uint32_t REGEL142;

_Atomic uint32_t REGEL143;

_Atomic uint32_t REGEL144;

_Atomic uint32_t REGEL145;

_Atomic uint32_t REGEL146;

_Atomic uint32_t REGEL147;

_Atomic uint32_t REGEL148;

_Atomic uint32_t REGEL149;

_Atomic uint32_t REGEL150;

_Atomic uint32_t REGEL151;

_Atomic uint32_t REGEL152;

_Atomic uint32_t REGEL153;

_Atomic uint32_t REGEL154;

_Atomic uint32_t REGEL155;

_Atomic uint32_t REGEL156;

_Atomic uint32_t REGEL157;

_Atomic uint32_t REGEL158;

_Atomic uint32_t REGEL159;

_Atomic uint32_t REGEL160;

_Atomic uint32_t REGEL161;

_Atomic uint32_t REGEL162;

_Atomic uint32_t REGEL163;

_Atomic uint32_t REGEL164;

_Atomic uint32_t REGEL165;

_Atomic uint32_t REGEL166;

_Atomic uint32_t REGEL167;

_Atomic uint32_t REGEL168;

_Atomic uint32_t REGEL169;

_Atomic uint32_t REGEL170;

_Atomic uint32_t REGEL171;

_Atomic uint32_t REGEL172;

_Atomic uint32_t REGEL173;

_Atomic uint32_t REGEL174;

_Atomic uint32_t REGEL175;

_Atomic uint32_t REGEL176;

_Atomic uint32_t REGEL177;

_Atomic uint32_t REGEL178;

_Atomic uint32_t REGEL179;

_Atomic uint32_t REGEL180;

_Atomic uint32_t REGEL181;

_Atomic uint32_t REGEL182;

_Atomic uint32_t REGEL183;

_Atomic uint32_t REGEL184;

_Atomic uint32_t REGEL185;

_Atomic uint32_t REGEL186;

_Atomic uint32_t REGEL187;

_Atomic uint32_t REGEL188;

_Atomic uint32_t REGEL189;

_Atomic uint32_t REGEL190;

_Atomic uint32_t REGEL191;

_Atomic uint32_t REGEL192;

_Atomic uint32_t REGEL193;

_Atomic uint32_t REGEL194;

_Atomic uint32_t REGEL195;

_Atomic uint32_t REGEL196;

_Atomic uint32_t REGEL197;

_Atomic uint32_t REGEL198;

_Atomic uint32_t REGEL199;

_Atomic uint32_t REGEL200;

_Atomic uint32_t REGEL201;

_Atomic uint32_t REGEL202;

_Atomic uint32_t REGEL203;

_Atomic uint32_t REGEL204;

_Atomic uint32_t REGEL205;

_Atomic uint32_t REGEL206;

_Atomic uint32_t REGEL207;

_Atomic uint32_t REGEL208;

_Atomic uint32_t REGEL209;

_Atomic uint32_t REGEL210;

_Atomic uint32_t REGEL211;

_Atomic uint32_t REGEL212;

_Atomic uint32_t REGEL213;

_Atomic uint32_t REGEL214;

_Atomic uint32_t REGEL215;

_Atomic uint32_t REGEL216;

_Atomic uint32_t REGEL217;

_Atomic uint32_t REGEL218;

_Atomic uint32_t REGEL219;

_Atomic uint32_t REGEL220;

_Atomic uint32_t REGEL221;

_Atomic uint32_t REGEL222;

_Atomic uint32_t REGEL223;

_Atomic uint32_t REGEL224;

_Atomic uint32_t REGEL225;

_Atomic uint32_t REGEL226;

_Atomic uint32_t REGEL227;

_Atomic uint32_t REGEL228;

_Atomic uint32_t REGEL229;

_Atomic uint32_t REGEL230;

_Atomic uint32_t REGEL231;

_Atomic uint32_t REGEL232;

_Atomic uint32_t REGEL233;

_Atomic uint32_t REGEL234;

_Atomic uint32_t REGEL235;

_Atomic uint32_t REGEL236;

_Atomic uint32_t REGEL237;

_Atomic uint32_t REGEL238;

_Atomic uint32_t REGEL239;

_Atomic uint32_t REGEL240;

_Atomic uint32_t REGEL241;

_Atomic uint32_t REGEL242;

_Atomic uint32_t REGEL243;

_Atomic uint32_t REGEL244;

_Atomic uint32_t REGEL245;

_Atomic uint32_t REGEL246;

_Atomic uint32_t REGEL247;

_Atomic uint32_t REGEL248;

_Atomic uint32_t REGEL249;

_Atomic uint32_t REGEL250;

_Atomic uint32_t REGEL251;

_Atomic uint32_t REGEL252;

_Atomic uint32_t REGEL253;

_Atomic uint32_t REGEL254;

_Atomic uint32_t REGEL255;

static uint32_t folge(uint32_t x) __attribute__((const)) __attribute__((unused));

_Noreturn void zaehler_streit(void);

static uint32_t ink_gesamt(void) __attribute__((unused));

static uint32_t ink_verworfen(void) __attribute__((unused));

static uint32_t ink_akzeptiert(void) __attribute__((unused));

static uint32_t ink_abgewiesen(void) __attribute__((unused));

static uint32_t ink_fehlform(void) __attribute__((unused));

static uint32_t ink_voll(void) __attribute__((unused));

static uint32_t ink_regelsumme(void) __attribute__((unused));

static uint32_t ink_r000(void) __attribute__((unused));

static uint32_t ink_r001(void) __attribute__((unused));

static uint32_t ink_r002(void) __attribute__((unused));

static uint32_t ink_r003(void) __attribute__((unused));

static uint32_t ink_r004(void) __attribute__((unused));

static uint32_t ink_r005(void) __attribute__((unused));

static uint32_t ink_r006(void) __attribute__((unused));

static uint32_t ink_r007(void) __attribute__((unused));

static uint32_t ink_r008(void) __attribute__((unused));

static uint32_t ink_r009(void) __attribute__((unused));

static uint32_t ink_r010(void) __attribute__((unused));

static uint32_t ink_r011(void) __attribute__((unused));

static uint32_t ink_r012(void) __attribute__((unused));

static uint32_t ink_r013(void) __attribute__((unused));

static uint32_t ink_r014(void) __attribute__((unused));

static uint32_t ink_r015(void) __attribute__((unused));

static uint32_t ink_r016(void) __attribute__((unused));

static uint32_t ink_r017(void) __attribute__((unused));

static uint32_t ink_r018(void) __attribute__((unused));

static uint32_t ink_r019(void) __attribute__((unused));

static uint32_t ink_r020(void) __attribute__((unused));

static uint32_t ink_r021(void) __attribute__((unused));

static uint32_t ink_r022(void) __attribute__((unused));

static uint32_t ink_r023(void) __attribute__((unused));

static uint32_t ink_r024(void) __attribute__((unused));

static uint32_t ink_r025(void) __attribute__((unused));

static uint32_t ink_r026(void) __attribute__((unused));

static uint32_t ink_r027(void) __attribute__((unused));

static uint32_t ink_r028(void) __attribute__((unused));

static uint32_t ink_r029(void) __attribute__((unused));

static uint32_t ink_r030(void) __attribute__((unused));

static uint32_t ink_r031(void) __attribute__((unused));

static uint32_t ink_r032(void) __attribute__((unused));

static uint32_t ink_r033(void) __attribute__((unused));

static uint32_t ink_r034(void) __attribute__((unused));

static uint32_t ink_r035(void) __attribute__((unused));

static uint32_t ink_r036(void) __attribute__((unused));

static uint32_t ink_r037(void) __attribute__((unused));

static uint32_t ink_r038(void) __attribute__((unused));

static uint32_t ink_r039(void) __attribute__((unused));

static uint32_t ink_r040(void) __attribute__((unused));

static uint32_t ink_r041(void) __attribute__((unused));

static uint32_t ink_r042(void) __attribute__((unused));

static uint32_t ink_r043(void) __attribute__((unused));

static uint32_t ink_r044(void) __attribute__((unused));

static uint32_t ink_r045(void) __attribute__((unused));

static uint32_t ink_r046(void) __attribute__((unused));

static uint32_t ink_r047(void) __attribute__((unused));

static uint32_t ink_r048(void) __attribute__((unused));

static uint32_t ink_r049(void) __attribute__((unused));

static uint32_t ink_r050(void) __attribute__((unused));

static uint32_t ink_r051(void) __attribute__((unused));

static uint32_t ink_r052(void) __attribute__((unused));

static uint32_t ink_r053(void) __attribute__((unused));

static uint32_t ink_r054(void) __attribute__((unused));

static uint32_t ink_r055(void) __attribute__((unused));

static uint32_t ink_r056(void) __attribute__((unused));

static uint32_t ink_r057(void) __attribute__((unused));

static uint32_t ink_r058(void) __attribute__((unused));

static uint32_t ink_r059(void) __attribute__((unused));

static uint32_t ink_r060(void) __attribute__((unused));

static uint32_t ink_r061(void) __attribute__((unused));

static uint32_t ink_r062(void) __attribute__((unused));

static uint32_t ink_r063(void) __attribute__((unused));

static uint32_t ink_r064(void) __attribute__((unused));

static uint32_t ink_r065(void) __attribute__((unused));

static uint32_t ink_r066(void) __attribute__((unused));

static uint32_t ink_r067(void) __attribute__((unused));

static uint32_t ink_r068(void) __attribute__((unused));

static uint32_t ink_r069(void) __attribute__((unused));

static uint32_t ink_r070(void) __attribute__((unused));

static uint32_t ink_r071(void) __attribute__((unused));

static uint32_t ink_r072(void) __attribute__((unused));

static uint32_t ink_r073(void) __attribute__((unused));

static uint32_t ink_r074(void) __attribute__((unused));

static uint32_t ink_r075(void) __attribute__((unused));

static uint32_t ink_r076(void) __attribute__((unused));

static uint32_t ink_r077(void) __attribute__((unused));

static uint32_t ink_r078(void) __attribute__((unused));

static uint32_t ink_r079(void) __attribute__((unused));

static uint32_t ink_r080(void) __attribute__((unused));

static uint32_t ink_r081(void) __attribute__((unused));

static uint32_t ink_r082(void) __attribute__((unused));

static uint32_t ink_r083(void) __attribute__((unused));

static uint32_t ink_r084(void) __attribute__((unused));

static uint32_t ink_r085(void) __attribute__((unused));

static uint32_t ink_r086(void) __attribute__((unused));

static uint32_t ink_r087(void) __attribute__((unused));

static uint32_t ink_r088(void) __attribute__((unused));

static uint32_t ink_r089(void) __attribute__((unused));

static uint32_t ink_r090(void) __attribute__((unused));

static uint32_t ink_r091(void) __attribute__((unused));

static uint32_t ink_r092(void) __attribute__((unused));

static uint32_t ink_r093(void) __attribute__((unused));

static uint32_t ink_r094(void) __attribute__((unused));

static uint32_t ink_r095(void) __attribute__((unused));

static uint32_t ink_r096(void) __attribute__((unused));

static uint32_t ink_r097(void) __attribute__((unused));

static uint32_t ink_r098(void) __attribute__((unused));

static uint32_t ink_r099(void) __attribute__((unused));

static uint32_t ink_r100(void) __attribute__((unused));

static uint32_t ink_r101(void) __attribute__((unused));

static uint32_t ink_r102(void) __attribute__((unused));

static uint32_t ink_r103(void) __attribute__((unused));

static uint32_t ink_r104(void) __attribute__((unused));

static uint32_t ink_r105(void) __attribute__((unused));

static uint32_t ink_r106(void) __attribute__((unused));

static uint32_t ink_r107(void) __attribute__((unused));

static uint32_t ink_r108(void) __attribute__((unused));

static uint32_t ink_r109(void) __attribute__((unused));

static uint32_t ink_r110(void) __attribute__((unused));

static uint32_t ink_r111(void) __attribute__((unused));

static uint32_t ink_r112(void) __attribute__((unused));

static uint32_t ink_r113(void) __attribute__((unused));

static uint32_t ink_r114(void) __attribute__((unused));

static uint32_t ink_r115(void) __attribute__((unused));

static uint32_t ink_r116(void) __attribute__((unused));

static uint32_t ink_r117(void) __attribute__((unused));

static uint32_t ink_r118(void) __attribute__((unused));

static uint32_t ink_r119(void) __attribute__((unused));

static uint32_t ink_r120(void) __attribute__((unused));

static uint32_t ink_r121(void) __attribute__((unused));

static uint32_t ink_r122(void) __attribute__((unused));

static uint32_t ink_r123(void) __attribute__((unused));

static uint32_t ink_r124(void) __attribute__((unused));

static uint32_t ink_r125(void) __attribute__((unused));

static uint32_t ink_r126(void) __attribute__((unused));

static uint32_t ink_r127(void) __attribute__((unused));

static uint32_t ink_r128(void) __attribute__((unused));

static uint32_t ink_r129(void) __attribute__((unused));

static uint32_t ink_r130(void) __attribute__((unused));

static uint32_t ink_r131(void) __attribute__((unused));

static uint32_t ink_r132(void) __attribute__((unused));

static uint32_t ink_r133(void) __attribute__((unused));

static uint32_t ink_r134(void) __attribute__((unused));

static uint32_t ink_r135(void) __attribute__((unused));

static uint32_t ink_r136(void) __attribute__((unused));

static uint32_t ink_r137(void) __attribute__((unused));

static uint32_t ink_r138(void) __attribute__((unused));

static uint32_t ink_r139(void) __attribute__((unused));

static uint32_t ink_r140(void) __attribute__((unused));

static uint32_t ink_r141(void) __attribute__((unused));

static uint32_t ink_r142(void) __attribute__((unused));

static uint32_t ink_r143(void) __attribute__((unused));

static uint32_t ink_r144(void) __attribute__((unused));

static uint32_t ink_r145(void) __attribute__((unused));

static uint32_t ink_r146(void) __attribute__((unused));

static uint32_t ink_r147(void) __attribute__((unused));

static uint32_t ink_r148(void) __attribute__((unused));

static uint32_t ink_r149(void) __attribute__((unused));

static uint32_t ink_r150(void) __attribute__((unused));

static uint32_t ink_r151(void) __attribute__((unused));

static uint32_t ink_r152(void) __attribute__((unused));

static uint32_t ink_r153(void) __attribute__((unused));

static uint32_t ink_r154(void) __attribute__((unused));

static uint32_t ink_r155(void) __attribute__((unused));

static uint32_t ink_r156(void) __attribute__((unused));

static uint32_t ink_r157(void) __attribute__((unused));

static uint32_t ink_r158(void) __attribute__((unused));

static uint32_t ink_r159(void) __attribute__((unused));

static uint32_t ink_r160(void) __attribute__((unused));

static uint32_t ink_r161(void) __attribute__((unused));

static uint32_t ink_r162(void) __attribute__((unused));

static uint32_t ink_r163(void) __attribute__((unused));

static uint32_t ink_r164(void) __attribute__((unused));

static uint32_t ink_r165(void) __attribute__((unused));

static uint32_t ink_r166(void) __attribute__((unused));

static uint32_t ink_r167(void) __attribute__((unused));

static uint32_t ink_r168(void) __attribute__((unused));

static uint32_t ink_r169(void) __attribute__((unused));

static uint32_t ink_r170(void) __attribute__((unused));

static uint32_t ink_r171(void) __attribute__((unused));

static uint32_t ink_r172(void) __attribute__((unused));

static uint32_t ink_r173(void) __attribute__((unused));

static uint32_t ink_r174(void) __attribute__((unused));

static uint32_t ink_r175(void) __attribute__((unused));

static uint32_t ink_r176(void) __attribute__((unused));

static uint32_t ink_r177(void) __attribute__((unused));

static uint32_t ink_r178(void) __attribute__((unused));

static uint32_t ink_r179(void) __attribute__((unused));

static uint32_t ink_r180(void) __attribute__((unused));

static uint32_t ink_r181(void) __attribute__((unused));

static uint32_t ink_r182(void) __attribute__((unused));

static uint32_t ink_r183(void) __attribute__((unused));

static uint32_t ink_r184(void) __attribute__((unused));

static uint32_t ink_r185(void) __attribute__((unused));

static uint32_t ink_r186(void) __attribute__((unused));

static uint32_t ink_r187(void) __attribute__((unused));

static uint32_t ink_r188(void) __attribute__((unused));

static uint32_t ink_r189(void) __attribute__((unused));

static uint32_t ink_r190(void) __attribute__((unused));

static uint32_t ink_r191(void) __attribute__((unused));

static uint32_t ink_r192(void) __attribute__((unused));

static uint32_t ink_r193(void) __attribute__((unused));

static uint32_t ink_r194(void) __attribute__((unused));

static uint32_t ink_r195(void) __attribute__((unused));

static uint32_t ink_r196(void) __attribute__((unused));

static uint32_t ink_r197(void) __attribute__((unused));

static uint32_t ink_r198(void) __attribute__((unused));

static uint32_t ink_r199(void) __attribute__((unused));

static uint32_t ink_r200(void) __attribute__((unused));

static uint32_t ink_r201(void) __attribute__((unused));

static uint32_t ink_r202(void) __attribute__((unused));

static uint32_t ink_r203(void) __attribute__((unused));

static uint32_t ink_r204(void) __attribute__((unused));

static uint32_t ink_r205(void) __attribute__((unused));

static uint32_t ink_r206(void) __attribute__((unused));

static uint32_t ink_r207(void) __attribute__((unused));

static uint32_t ink_r208(void) __attribute__((unused));

static uint32_t ink_r209(void) __attribute__((unused));

static uint32_t ink_r210(void) __attribute__((unused));

static uint32_t ink_r211(void) __attribute__((unused));

static uint32_t ink_r212(void) __attribute__((unused));

static uint32_t ink_r213(void) __attribute__((unused));

static uint32_t ink_r214(void) __attribute__((unused));

static uint32_t ink_r215(void) __attribute__((unused));

static uint32_t ink_r216(void) __attribute__((unused));

static uint32_t ink_r217(void) __attribute__((unused));

static uint32_t ink_r218(void) __attribute__((unused));

static uint32_t ink_r219(void) __attribute__((unused));

static uint32_t ink_r220(void) __attribute__((unused));

static uint32_t ink_r221(void) __attribute__((unused));

static uint32_t ink_r222(void) __attribute__((unused));

static uint32_t ink_r223(void) __attribute__((unused));

static uint32_t ink_r224(void) __attribute__((unused));

static uint32_t ink_r225(void) __attribute__((unused));

static uint32_t ink_r226(void) __attribute__((unused));

static uint32_t ink_r227(void) __attribute__((unused));

static uint32_t ink_r228(void) __attribute__((unused));

static uint32_t ink_r229(void) __attribute__((unused));

static uint32_t ink_r230(void) __attribute__((unused));

static uint32_t ink_r231(void) __attribute__((unused));

static uint32_t ink_r232(void) __attribute__((unused));

static uint32_t ink_r233(void) __attribute__((unused));

static uint32_t ink_r234(void) __attribute__((unused));

static uint32_t ink_r235(void) __attribute__((unused));

static uint32_t ink_r236(void) __attribute__((unused));

static uint32_t ink_r237(void) __attribute__((unused));

static uint32_t ink_r238(void) __attribute__((unused));

static uint32_t ink_r239(void) __attribute__((unused));

static uint32_t ink_r240(void) __attribute__((unused));

static uint32_t ink_r241(void) __attribute__((unused));

static uint32_t ink_r242(void) __attribute__((unused));

static uint32_t ink_r243(void) __attribute__((unused));

static uint32_t ink_r244(void) __attribute__((unused));

static uint32_t ink_r245(void) __attribute__((unused));

static uint32_t ink_r246(void) __attribute__((unused));

static uint32_t ink_r247(void) __attribute__((unused));

static uint32_t ink_r248(void) __attribute__((unused));

static uint32_t ink_r249(void) __attribute__((unused));

static uint32_t ink_r250(void) __attribute__((unused));

static uint32_t ink_r251(void) __attribute__((unused));

static uint32_t ink_r252(void) __attribute__((unused));

static uint32_t ink_r253(void) __attribute__((unused));

static uint32_t ink_r254(void) __attribute__((unused));

static uint32_t ink_r255(void) __attribute__((unused));

void zaehle_urteil(uint32_t u);

void zaehle_regel(uint32_t r);

void zaehle_fehl(void);

void zaehle_voll(void);

uint32_t lies_zaehler(uint32_t welcher) __attribute__((pure));

uint32_t lies_regel_treffer(uint32_t r) __attribute__((pure));

static uint32_t folge(uint32_t x) {
    return (uint32_t)(((uint32_t)(x) + (uint32_t)(1)));
}

static uint32_t ink_gesamt(void) {
    /* GESAMT exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_gesamt;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&GESAMT, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &GESAMT, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_gesamt = _cx1;
    }
    return alt_gesamt;
}

static uint32_t ink_verworfen(void) {
    /* VERWORFEN exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_verworfen;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&VERWORFEN, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &VERWORFEN, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_verworfen = _cx1;
    }
    return alt_verworfen;
}

static uint32_t ink_akzeptiert(void) {
    /* AKZEPTIERT exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_akzeptiert;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&AKZEPTIERT, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &AKZEPTIERT, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_akzeptiert = _cx1;
    }
    return alt_akzeptiert;
}

static uint32_t ink_abgewiesen(void) {
    /* ABGEWIESEN exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_abgewiesen;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&ABGEWIESEN, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &ABGEWIESEN, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_abgewiesen = _cx1;
    }
    return alt_abgewiesen;
}

static uint32_t ink_fehlform(void) {
    /* FEHLFORM exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_fehlform;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&FEHLFORM, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &FEHLFORM, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_fehlform = _cx1;
    }
    return alt_fehlform;
}

static uint32_t ink_voll(void) {
    /* VOLL exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_voll;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&VOLL, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &VOLL, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_voll = _cx1;
    }
    return alt_voll;
}

static uint32_t ink_regelsumme(void) {
    /* REGELSUMME exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt_regelsumme;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGELSUMME, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGELSUMME, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt_regelsumme = _cx1;
    }
    return alt_regelsumme;
}

static uint32_t ink_r000(void) {
    /* REGEL000 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt000;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL000, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL000, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt000 = _cx1;
    }
    return alt000;
}

static uint32_t ink_r001(void) {
    /* REGEL001 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt001;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL001, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL001, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt001 = _cx1;
    }
    return alt001;
}

static uint32_t ink_r002(void) {
    /* REGEL002 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt002;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL002, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL002, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt002 = _cx1;
    }
    return alt002;
}

static uint32_t ink_r003(void) {
    /* REGEL003 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt003;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL003, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL003, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt003 = _cx1;
    }
    return alt003;
}

static uint32_t ink_r004(void) {
    /* REGEL004 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt004;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL004, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL004, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt004 = _cx1;
    }
    return alt004;
}

static uint32_t ink_r005(void) {
    /* REGEL005 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt005;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL005, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL005, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt005 = _cx1;
    }
    return alt005;
}

static uint32_t ink_r006(void) {
    /* REGEL006 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt006;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL006, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL006, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt006 = _cx1;
    }
    return alt006;
}

static uint32_t ink_r007(void) {
    /* REGEL007 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt007;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL007, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL007, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt007 = _cx1;
    }
    return alt007;
}

static uint32_t ink_r008(void) {
    /* REGEL008 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt008;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL008, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL008, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt008 = _cx1;
    }
    return alt008;
}

static uint32_t ink_r009(void) {
    /* REGEL009 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt009;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL009, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL009, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt009 = _cx1;
    }
    return alt009;
}

static uint32_t ink_r010(void) {
    /* REGEL010 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt010;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL010, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL010, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt010 = _cx1;
    }
    return alt010;
}

static uint32_t ink_r011(void) {
    /* REGEL011 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt011;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL011, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL011, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt011 = _cx1;
    }
    return alt011;
}

static uint32_t ink_r012(void) {
    /* REGEL012 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt012;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL012, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL012, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt012 = _cx1;
    }
    return alt012;
}

static uint32_t ink_r013(void) {
    /* REGEL013 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt013;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL013, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL013, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt013 = _cx1;
    }
    return alt013;
}

static uint32_t ink_r014(void) {
    /* REGEL014 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt014;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL014, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL014, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt014 = _cx1;
    }
    return alt014;
}

static uint32_t ink_r015(void) {
    /* REGEL015 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt015;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL015, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL015, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt015 = _cx1;
    }
    return alt015;
}

static uint32_t ink_r016(void) {
    /* REGEL016 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt016;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL016, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL016, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt016 = _cx1;
    }
    return alt016;
}

static uint32_t ink_r017(void) {
    /* REGEL017 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt017;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL017, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL017, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt017 = _cx1;
    }
    return alt017;
}

static uint32_t ink_r018(void) {
    /* REGEL018 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt018;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL018, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL018, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt018 = _cx1;
    }
    return alt018;
}

static uint32_t ink_r019(void) {
    /* REGEL019 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt019;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL019, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL019, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt019 = _cx1;
    }
    return alt019;
}

static uint32_t ink_r020(void) {
    /* REGEL020 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt020;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL020, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL020, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt020 = _cx1;
    }
    return alt020;
}

static uint32_t ink_r021(void) {
    /* REGEL021 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt021;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL021, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL021, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt021 = _cx1;
    }
    return alt021;
}

static uint32_t ink_r022(void) {
    /* REGEL022 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt022;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL022, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL022, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt022 = _cx1;
    }
    return alt022;
}

static uint32_t ink_r023(void) {
    /* REGEL023 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt023;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL023, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL023, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt023 = _cx1;
    }
    return alt023;
}

static uint32_t ink_r024(void) {
    /* REGEL024 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt024;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL024, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL024, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt024 = _cx1;
    }
    return alt024;
}

static uint32_t ink_r025(void) {
    /* REGEL025 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt025;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL025, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL025, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt025 = _cx1;
    }
    return alt025;
}

static uint32_t ink_r026(void) {
    /* REGEL026 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt026;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL026, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL026, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt026 = _cx1;
    }
    return alt026;
}

static uint32_t ink_r027(void) {
    /* REGEL027 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt027;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL027, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL027, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt027 = _cx1;
    }
    return alt027;
}

static uint32_t ink_r028(void) {
    /* REGEL028 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt028;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL028, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL028, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt028 = _cx1;
    }
    return alt028;
}

static uint32_t ink_r029(void) {
    /* REGEL029 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt029;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL029, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL029, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt029 = _cx1;
    }
    return alt029;
}

static uint32_t ink_r030(void) {
    /* REGEL030 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt030;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL030, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL030, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt030 = _cx1;
    }
    return alt030;
}

static uint32_t ink_r031(void) {
    /* REGEL031 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt031;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL031, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL031, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt031 = _cx1;
    }
    return alt031;
}

static uint32_t ink_r032(void) {
    /* REGEL032 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt032;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL032, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL032, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt032 = _cx1;
    }
    return alt032;
}

static uint32_t ink_r033(void) {
    /* REGEL033 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt033;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL033, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL033, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt033 = _cx1;
    }
    return alt033;
}

static uint32_t ink_r034(void) {
    /* REGEL034 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt034;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL034, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL034, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt034 = _cx1;
    }
    return alt034;
}

static uint32_t ink_r035(void) {
    /* REGEL035 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt035;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL035, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL035, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt035 = _cx1;
    }
    return alt035;
}

static uint32_t ink_r036(void) {
    /* REGEL036 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt036;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL036, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL036, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt036 = _cx1;
    }
    return alt036;
}

static uint32_t ink_r037(void) {
    /* REGEL037 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt037;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL037, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL037, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt037 = _cx1;
    }
    return alt037;
}

static uint32_t ink_r038(void) {
    /* REGEL038 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt038;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL038, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL038, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt038 = _cx1;
    }
    return alt038;
}

static uint32_t ink_r039(void) {
    /* REGEL039 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt039;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL039, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL039, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt039 = _cx1;
    }
    return alt039;
}

static uint32_t ink_r040(void) {
    /* REGEL040 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt040;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL040, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL040, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt040 = _cx1;
    }
    return alt040;
}

static uint32_t ink_r041(void) {
    /* REGEL041 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt041;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL041, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL041, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt041 = _cx1;
    }
    return alt041;
}

static uint32_t ink_r042(void) {
    /* REGEL042 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt042;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL042, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL042, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt042 = _cx1;
    }
    return alt042;
}

static uint32_t ink_r043(void) {
    /* REGEL043 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt043;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL043, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL043, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt043 = _cx1;
    }
    return alt043;
}

static uint32_t ink_r044(void) {
    /* REGEL044 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt044;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL044, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL044, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt044 = _cx1;
    }
    return alt044;
}

static uint32_t ink_r045(void) {
    /* REGEL045 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt045;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL045, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL045, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt045 = _cx1;
    }
    return alt045;
}

static uint32_t ink_r046(void) {
    /* REGEL046 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt046;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL046, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL046, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt046 = _cx1;
    }
    return alt046;
}

static uint32_t ink_r047(void) {
    /* REGEL047 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt047;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL047, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL047, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt047 = _cx1;
    }
    return alt047;
}

static uint32_t ink_r048(void) {
    /* REGEL048 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt048;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL048, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL048, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt048 = _cx1;
    }
    return alt048;
}

static uint32_t ink_r049(void) {
    /* REGEL049 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt049;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL049, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL049, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt049 = _cx1;
    }
    return alt049;
}

static uint32_t ink_r050(void) {
    /* REGEL050 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt050;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL050, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL050, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt050 = _cx1;
    }
    return alt050;
}

static uint32_t ink_r051(void) {
    /* REGEL051 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt051;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL051, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL051, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt051 = _cx1;
    }
    return alt051;
}

static uint32_t ink_r052(void) {
    /* REGEL052 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt052;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL052, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL052, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt052 = _cx1;
    }
    return alt052;
}

static uint32_t ink_r053(void) {
    /* REGEL053 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt053;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL053, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL053, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt053 = _cx1;
    }
    return alt053;
}

static uint32_t ink_r054(void) {
    /* REGEL054 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt054;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL054, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL054, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt054 = _cx1;
    }
    return alt054;
}

static uint32_t ink_r055(void) {
    /* REGEL055 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt055;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL055, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL055, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt055 = _cx1;
    }
    return alt055;
}

static uint32_t ink_r056(void) {
    /* REGEL056 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt056;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL056, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL056, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt056 = _cx1;
    }
    return alt056;
}

static uint32_t ink_r057(void) {
    /* REGEL057 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt057;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL057, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL057, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt057 = _cx1;
    }
    return alt057;
}

static uint32_t ink_r058(void) {
    /* REGEL058 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt058;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL058, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL058, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt058 = _cx1;
    }
    return alt058;
}

static uint32_t ink_r059(void) {
    /* REGEL059 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt059;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL059, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL059, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt059 = _cx1;
    }
    return alt059;
}

static uint32_t ink_r060(void) {
    /* REGEL060 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt060;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL060, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL060, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt060 = _cx1;
    }
    return alt060;
}

static uint32_t ink_r061(void) {
    /* REGEL061 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt061;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL061, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL061, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt061 = _cx1;
    }
    return alt061;
}

static uint32_t ink_r062(void) {
    /* REGEL062 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt062;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL062, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL062, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt062 = _cx1;
    }
    return alt062;
}

static uint32_t ink_r063(void) {
    /* REGEL063 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt063;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL063, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL063, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt063 = _cx1;
    }
    return alt063;
}

static uint32_t ink_r064(void) {
    /* REGEL064 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt064;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL064, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL064, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt064 = _cx1;
    }
    return alt064;
}

static uint32_t ink_r065(void) {
    /* REGEL065 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt065;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL065, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL065, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt065 = _cx1;
    }
    return alt065;
}

static uint32_t ink_r066(void) {
    /* REGEL066 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt066;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL066, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL066, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt066 = _cx1;
    }
    return alt066;
}

static uint32_t ink_r067(void) {
    /* REGEL067 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt067;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL067, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL067, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt067 = _cx1;
    }
    return alt067;
}

static uint32_t ink_r068(void) {
    /* REGEL068 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt068;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL068, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL068, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt068 = _cx1;
    }
    return alt068;
}

static uint32_t ink_r069(void) {
    /* REGEL069 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt069;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL069, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL069, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt069 = _cx1;
    }
    return alt069;
}

static uint32_t ink_r070(void) {
    /* REGEL070 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt070;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL070, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL070, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt070 = _cx1;
    }
    return alt070;
}

static uint32_t ink_r071(void) {
    /* REGEL071 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt071;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL071, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL071, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt071 = _cx1;
    }
    return alt071;
}

static uint32_t ink_r072(void) {
    /* REGEL072 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt072;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL072, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL072, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt072 = _cx1;
    }
    return alt072;
}

static uint32_t ink_r073(void) {
    /* REGEL073 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt073;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL073, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL073, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt073 = _cx1;
    }
    return alt073;
}

static uint32_t ink_r074(void) {
    /* REGEL074 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt074;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL074, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL074, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt074 = _cx1;
    }
    return alt074;
}

static uint32_t ink_r075(void) {
    /* REGEL075 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt075;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL075, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL075, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt075 = _cx1;
    }
    return alt075;
}

static uint32_t ink_r076(void) {
    /* REGEL076 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt076;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL076, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL076, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt076 = _cx1;
    }
    return alt076;
}

static uint32_t ink_r077(void) {
    /* REGEL077 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt077;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL077, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL077, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt077 = _cx1;
    }
    return alt077;
}

static uint32_t ink_r078(void) {
    /* REGEL078 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt078;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL078, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL078, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt078 = _cx1;
    }
    return alt078;
}

static uint32_t ink_r079(void) {
    /* REGEL079 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt079;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL079, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL079, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt079 = _cx1;
    }
    return alt079;
}

static uint32_t ink_r080(void) {
    /* REGEL080 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt080;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL080, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL080, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt080 = _cx1;
    }
    return alt080;
}

static uint32_t ink_r081(void) {
    /* REGEL081 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt081;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL081, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL081, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt081 = _cx1;
    }
    return alt081;
}

static uint32_t ink_r082(void) {
    /* REGEL082 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt082;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL082, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL082, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt082 = _cx1;
    }
    return alt082;
}

static uint32_t ink_r083(void) {
    /* REGEL083 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt083;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL083, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL083, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt083 = _cx1;
    }
    return alt083;
}

static uint32_t ink_r084(void) {
    /* REGEL084 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt084;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL084, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL084, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt084 = _cx1;
    }
    return alt084;
}

static uint32_t ink_r085(void) {
    /* REGEL085 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt085;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL085, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL085, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt085 = _cx1;
    }
    return alt085;
}

static uint32_t ink_r086(void) {
    /* REGEL086 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt086;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL086, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL086, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt086 = _cx1;
    }
    return alt086;
}

static uint32_t ink_r087(void) {
    /* REGEL087 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt087;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL087, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL087, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt087 = _cx1;
    }
    return alt087;
}

static uint32_t ink_r088(void) {
    /* REGEL088 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt088;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL088, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL088, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt088 = _cx1;
    }
    return alt088;
}

static uint32_t ink_r089(void) {
    /* REGEL089 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt089;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL089, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL089, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt089 = _cx1;
    }
    return alt089;
}

static uint32_t ink_r090(void) {
    /* REGEL090 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt090;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL090, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL090, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt090 = _cx1;
    }
    return alt090;
}

static uint32_t ink_r091(void) {
    /* REGEL091 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt091;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL091, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL091, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt091 = _cx1;
    }
    return alt091;
}

static uint32_t ink_r092(void) {
    /* REGEL092 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt092;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL092, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL092, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt092 = _cx1;
    }
    return alt092;
}

static uint32_t ink_r093(void) {
    /* REGEL093 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt093;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL093, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL093, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt093 = _cx1;
    }
    return alt093;
}

static uint32_t ink_r094(void) {
    /* REGEL094 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt094;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL094, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL094, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt094 = _cx1;
    }
    return alt094;
}

static uint32_t ink_r095(void) {
    /* REGEL095 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt095;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL095, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL095, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt095 = _cx1;
    }
    return alt095;
}

static uint32_t ink_r096(void) {
    /* REGEL096 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt096;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL096, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL096, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt096 = _cx1;
    }
    return alt096;
}

static uint32_t ink_r097(void) {
    /* REGEL097 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt097;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL097, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL097, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt097 = _cx1;
    }
    return alt097;
}

static uint32_t ink_r098(void) {
    /* REGEL098 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt098;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL098, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL098, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt098 = _cx1;
    }
    return alt098;
}

static uint32_t ink_r099(void) {
    /* REGEL099 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt099;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL099, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL099, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt099 = _cx1;
    }
    return alt099;
}

static uint32_t ink_r100(void) {
    /* REGEL100 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt100;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL100, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL100, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt100 = _cx1;
    }
    return alt100;
}

static uint32_t ink_r101(void) {
    /* REGEL101 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt101;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL101, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL101, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt101 = _cx1;
    }
    return alt101;
}

static uint32_t ink_r102(void) {
    /* REGEL102 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt102;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL102, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL102, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt102 = _cx1;
    }
    return alt102;
}

static uint32_t ink_r103(void) {
    /* REGEL103 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt103;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL103, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL103, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt103 = _cx1;
    }
    return alt103;
}

static uint32_t ink_r104(void) {
    /* REGEL104 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt104;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL104, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL104, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt104 = _cx1;
    }
    return alt104;
}

static uint32_t ink_r105(void) {
    /* REGEL105 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt105;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL105, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL105, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt105 = _cx1;
    }
    return alt105;
}

static uint32_t ink_r106(void) {
    /* REGEL106 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt106;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL106, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL106, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt106 = _cx1;
    }
    return alt106;
}

static uint32_t ink_r107(void) {
    /* REGEL107 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt107;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL107, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL107, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt107 = _cx1;
    }
    return alt107;
}

static uint32_t ink_r108(void) {
    /* REGEL108 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt108;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL108, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL108, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt108 = _cx1;
    }
    return alt108;
}

static uint32_t ink_r109(void) {
    /* REGEL109 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt109;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL109, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL109, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt109 = _cx1;
    }
    return alt109;
}

static uint32_t ink_r110(void) {
    /* REGEL110 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt110;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL110, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL110, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt110 = _cx1;
    }
    return alt110;
}

static uint32_t ink_r111(void) {
    /* REGEL111 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt111;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL111, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL111, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt111 = _cx1;
    }
    return alt111;
}

static uint32_t ink_r112(void) {
    /* REGEL112 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt112;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL112, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL112, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt112 = _cx1;
    }
    return alt112;
}

static uint32_t ink_r113(void) {
    /* REGEL113 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt113;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL113, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL113, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt113 = _cx1;
    }
    return alt113;
}

static uint32_t ink_r114(void) {
    /* REGEL114 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt114;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL114, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL114, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt114 = _cx1;
    }
    return alt114;
}

static uint32_t ink_r115(void) {
    /* REGEL115 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt115;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL115, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL115, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt115 = _cx1;
    }
    return alt115;
}

static uint32_t ink_r116(void) {
    /* REGEL116 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt116;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL116, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL116, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt116 = _cx1;
    }
    return alt116;
}

static uint32_t ink_r117(void) {
    /* REGEL117 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt117;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL117, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL117, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt117 = _cx1;
    }
    return alt117;
}

static uint32_t ink_r118(void) {
    /* REGEL118 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt118;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL118, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL118, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt118 = _cx1;
    }
    return alt118;
}

static uint32_t ink_r119(void) {
    /* REGEL119 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt119;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL119, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL119, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt119 = _cx1;
    }
    return alt119;
}

static uint32_t ink_r120(void) {
    /* REGEL120 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt120;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL120, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL120, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt120 = _cx1;
    }
    return alt120;
}

static uint32_t ink_r121(void) {
    /* REGEL121 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt121;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL121, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL121, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt121 = _cx1;
    }
    return alt121;
}

static uint32_t ink_r122(void) {
    /* REGEL122 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt122;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL122, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL122, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt122 = _cx1;
    }
    return alt122;
}

static uint32_t ink_r123(void) {
    /* REGEL123 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt123;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL123, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL123, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt123 = _cx1;
    }
    return alt123;
}

static uint32_t ink_r124(void) {
    /* REGEL124 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt124;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL124, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL124, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt124 = _cx1;
    }
    return alt124;
}

static uint32_t ink_r125(void) {
    /* REGEL125 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt125;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL125, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL125, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt125 = _cx1;
    }
    return alt125;
}

static uint32_t ink_r126(void) {
    /* REGEL126 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt126;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL126, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL126, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt126 = _cx1;
    }
    return alt126;
}

static uint32_t ink_r127(void) {
    /* REGEL127 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt127;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL127, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL127, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt127 = _cx1;
    }
    return alt127;
}

static uint32_t ink_r128(void) {
    /* REGEL128 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt128;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL128, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL128, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt128 = _cx1;
    }
    return alt128;
}

static uint32_t ink_r129(void) {
    /* REGEL129 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt129;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL129, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL129, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt129 = _cx1;
    }
    return alt129;
}

static uint32_t ink_r130(void) {
    /* REGEL130 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt130;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL130, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL130, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt130 = _cx1;
    }
    return alt130;
}

static uint32_t ink_r131(void) {
    /* REGEL131 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt131;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL131, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL131, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt131 = _cx1;
    }
    return alt131;
}

static uint32_t ink_r132(void) {
    /* REGEL132 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt132;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL132, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL132, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt132 = _cx1;
    }
    return alt132;
}

static uint32_t ink_r133(void) {
    /* REGEL133 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt133;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL133, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL133, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt133 = _cx1;
    }
    return alt133;
}

static uint32_t ink_r134(void) {
    /* REGEL134 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt134;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL134, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL134, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt134 = _cx1;
    }
    return alt134;
}

static uint32_t ink_r135(void) {
    /* REGEL135 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt135;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL135, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL135, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt135 = _cx1;
    }
    return alt135;
}

static uint32_t ink_r136(void) {
    /* REGEL136 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt136;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL136, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL136, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt136 = _cx1;
    }
    return alt136;
}

static uint32_t ink_r137(void) {
    /* REGEL137 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt137;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL137, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL137, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt137 = _cx1;
    }
    return alt137;
}

static uint32_t ink_r138(void) {
    /* REGEL138 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt138;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL138, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL138, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt138 = _cx1;
    }
    return alt138;
}

static uint32_t ink_r139(void) {
    /* REGEL139 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt139;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL139, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL139, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt139 = _cx1;
    }
    return alt139;
}

static uint32_t ink_r140(void) {
    /* REGEL140 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt140;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL140, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL140, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt140 = _cx1;
    }
    return alt140;
}

static uint32_t ink_r141(void) {
    /* REGEL141 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt141;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL141, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL141, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt141 = _cx1;
    }
    return alt141;
}

static uint32_t ink_r142(void) {
    /* REGEL142 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt142;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL142, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL142, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt142 = _cx1;
    }
    return alt142;
}

static uint32_t ink_r143(void) {
    /* REGEL143 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt143;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL143, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL143, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt143 = _cx1;
    }
    return alt143;
}

static uint32_t ink_r144(void) {
    /* REGEL144 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt144;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL144, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL144, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt144 = _cx1;
    }
    return alt144;
}

static uint32_t ink_r145(void) {
    /* REGEL145 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt145;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL145, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL145, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt145 = _cx1;
    }
    return alt145;
}

static uint32_t ink_r146(void) {
    /* REGEL146 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt146;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL146, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL146, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt146 = _cx1;
    }
    return alt146;
}

static uint32_t ink_r147(void) {
    /* REGEL147 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt147;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL147, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL147, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt147 = _cx1;
    }
    return alt147;
}

static uint32_t ink_r148(void) {
    /* REGEL148 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt148;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL148, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL148, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt148 = _cx1;
    }
    return alt148;
}

static uint32_t ink_r149(void) {
    /* REGEL149 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt149;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL149, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL149, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt149 = _cx1;
    }
    return alt149;
}

static uint32_t ink_r150(void) {
    /* REGEL150 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt150;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL150, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL150, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt150 = _cx1;
    }
    return alt150;
}

static uint32_t ink_r151(void) {
    /* REGEL151 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt151;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL151, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL151, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt151 = _cx1;
    }
    return alt151;
}

static uint32_t ink_r152(void) {
    /* REGEL152 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt152;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL152, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL152, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt152 = _cx1;
    }
    return alt152;
}

static uint32_t ink_r153(void) {
    /* REGEL153 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt153;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL153, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL153, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt153 = _cx1;
    }
    return alt153;
}

static uint32_t ink_r154(void) {
    /* REGEL154 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt154;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL154, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL154, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt154 = _cx1;
    }
    return alt154;
}

static uint32_t ink_r155(void) {
    /* REGEL155 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt155;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL155, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL155, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt155 = _cx1;
    }
    return alt155;
}

static uint32_t ink_r156(void) {
    /* REGEL156 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt156;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL156, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL156, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt156 = _cx1;
    }
    return alt156;
}

static uint32_t ink_r157(void) {
    /* REGEL157 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt157;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL157, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL157, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt157 = _cx1;
    }
    return alt157;
}

static uint32_t ink_r158(void) {
    /* REGEL158 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt158;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL158, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL158, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt158 = _cx1;
    }
    return alt158;
}

static uint32_t ink_r159(void) {
    /* REGEL159 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt159;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL159, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL159, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt159 = _cx1;
    }
    return alt159;
}

static uint32_t ink_r160(void) {
    /* REGEL160 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt160;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL160, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL160, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt160 = _cx1;
    }
    return alt160;
}

static uint32_t ink_r161(void) {
    /* REGEL161 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt161;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL161, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL161, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt161 = _cx1;
    }
    return alt161;
}

static uint32_t ink_r162(void) {
    /* REGEL162 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt162;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL162, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL162, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt162 = _cx1;
    }
    return alt162;
}

static uint32_t ink_r163(void) {
    /* REGEL163 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt163;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL163, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL163, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt163 = _cx1;
    }
    return alt163;
}

static uint32_t ink_r164(void) {
    /* REGEL164 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt164;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL164, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL164, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt164 = _cx1;
    }
    return alt164;
}

static uint32_t ink_r165(void) {
    /* REGEL165 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt165;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL165, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL165, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt165 = _cx1;
    }
    return alt165;
}

static uint32_t ink_r166(void) {
    /* REGEL166 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt166;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL166, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL166, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt166 = _cx1;
    }
    return alt166;
}

static uint32_t ink_r167(void) {
    /* REGEL167 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt167;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL167, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL167, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt167 = _cx1;
    }
    return alt167;
}

static uint32_t ink_r168(void) {
    /* REGEL168 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt168;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL168, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL168, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt168 = _cx1;
    }
    return alt168;
}

static uint32_t ink_r169(void) {
    /* REGEL169 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt169;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL169, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL169, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt169 = _cx1;
    }
    return alt169;
}

static uint32_t ink_r170(void) {
    /* REGEL170 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt170;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL170, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL170, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt170 = _cx1;
    }
    return alt170;
}

static uint32_t ink_r171(void) {
    /* REGEL171 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt171;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL171, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL171, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt171 = _cx1;
    }
    return alt171;
}

static uint32_t ink_r172(void) {
    /* REGEL172 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt172;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL172, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL172, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt172 = _cx1;
    }
    return alt172;
}

static uint32_t ink_r173(void) {
    /* REGEL173 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt173;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL173, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL173, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt173 = _cx1;
    }
    return alt173;
}

static uint32_t ink_r174(void) {
    /* REGEL174 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt174;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL174, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL174, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt174 = _cx1;
    }
    return alt174;
}

static uint32_t ink_r175(void) {
    /* REGEL175 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt175;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL175, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL175, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt175 = _cx1;
    }
    return alt175;
}

static uint32_t ink_r176(void) {
    /* REGEL176 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt176;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL176, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL176, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt176 = _cx1;
    }
    return alt176;
}

static uint32_t ink_r177(void) {
    /* REGEL177 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt177;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL177, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL177, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt177 = _cx1;
    }
    return alt177;
}

static uint32_t ink_r178(void) {
    /* REGEL178 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt178;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL178, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL178, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt178 = _cx1;
    }
    return alt178;
}

static uint32_t ink_r179(void) {
    /* REGEL179 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt179;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL179, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL179, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt179 = _cx1;
    }
    return alt179;
}

static uint32_t ink_r180(void) {
    /* REGEL180 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt180;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL180, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL180, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt180 = _cx1;
    }
    return alt180;
}

static uint32_t ink_r181(void) {
    /* REGEL181 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt181;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL181, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL181, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt181 = _cx1;
    }
    return alt181;
}

static uint32_t ink_r182(void) {
    /* REGEL182 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt182;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL182, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL182, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt182 = _cx1;
    }
    return alt182;
}

static uint32_t ink_r183(void) {
    /* REGEL183 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt183;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL183, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL183, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt183 = _cx1;
    }
    return alt183;
}

static uint32_t ink_r184(void) {
    /* REGEL184 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt184;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL184, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL184, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt184 = _cx1;
    }
    return alt184;
}

static uint32_t ink_r185(void) {
    /* REGEL185 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt185;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL185, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL185, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt185 = _cx1;
    }
    return alt185;
}

static uint32_t ink_r186(void) {
    /* REGEL186 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt186;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL186, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL186, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt186 = _cx1;
    }
    return alt186;
}

static uint32_t ink_r187(void) {
    /* REGEL187 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt187;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL187, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL187, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt187 = _cx1;
    }
    return alt187;
}

static uint32_t ink_r188(void) {
    /* REGEL188 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt188;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL188, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL188, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt188 = _cx1;
    }
    return alt188;
}

static uint32_t ink_r189(void) {
    /* REGEL189 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt189;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL189, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL189, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt189 = _cx1;
    }
    return alt189;
}

static uint32_t ink_r190(void) {
    /* REGEL190 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt190;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL190, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL190, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt190 = _cx1;
    }
    return alt190;
}

static uint32_t ink_r191(void) {
    /* REGEL191 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt191;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL191, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL191, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt191 = _cx1;
    }
    return alt191;
}

static uint32_t ink_r192(void) {
    /* REGEL192 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt192;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL192, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL192, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt192 = _cx1;
    }
    return alt192;
}

static uint32_t ink_r193(void) {
    /* REGEL193 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt193;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL193, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL193, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt193 = _cx1;
    }
    return alt193;
}

static uint32_t ink_r194(void) {
    /* REGEL194 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt194;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL194, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL194, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt194 = _cx1;
    }
    return alt194;
}

static uint32_t ink_r195(void) {
    /* REGEL195 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt195;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL195, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL195, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt195 = _cx1;
    }
    return alt195;
}

static uint32_t ink_r196(void) {
    /* REGEL196 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt196;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL196, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL196, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt196 = _cx1;
    }
    return alt196;
}

static uint32_t ink_r197(void) {
    /* REGEL197 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt197;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL197, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL197, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt197 = _cx1;
    }
    return alt197;
}

static uint32_t ink_r198(void) {
    /* REGEL198 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt198;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL198, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL198, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt198 = _cx1;
    }
    return alt198;
}

static uint32_t ink_r199(void) {
    /* REGEL199 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt199;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL199, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL199, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt199 = _cx1;
    }
    return alt199;
}

static uint32_t ink_r200(void) {
    /* REGEL200 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt200;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL200, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL200, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt200 = _cx1;
    }
    return alt200;
}

static uint32_t ink_r201(void) {
    /* REGEL201 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt201;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL201, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL201, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt201 = _cx1;
    }
    return alt201;
}

static uint32_t ink_r202(void) {
    /* REGEL202 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt202;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL202, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL202, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt202 = _cx1;
    }
    return alt202;
}

static uint32_t ink_r203(void) {
    /* REGEL203 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt203;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL203, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL203, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt203 = _cx1;
    }
    return alt203;
}

static uint32_t ink_r204(void) {
    /* REGEL204 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt204;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL204, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL204, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt204 = _cx1;
    }
    return alt204;
}

static uint32_t ink_r205(void) {
    /* REGEL205 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt205;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL205, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL205, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt205 = _cx1;
    }
    return alt205;
}

static uint32_t ink_r206(void) {
    /* REGEL206 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt206;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL206, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL206, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt206 = _cx1;
    }
    return alt206;
}

static uint32_t ink_r207(void) {
    /* REGEL207 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt207;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL207, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL207, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt207 = _cx1;
    }
    return alt207;
}

static uint32_t ink_r208(void) {
    /* REGEL208 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt208;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL208, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL208, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt208 = _cx1;
    }
    return alt208;
}

static uint32_t ink_r209(void) {
    /* REGEL209 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt209;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL209, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL209, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt209 = _cx1;
    }
    return alt209;
}

static uint32_t ink_r210(void) {
    /* REGEL210 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt210;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL210, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL210, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt210 = _cx1;
    }
    return alt210;
}

static uint32_t ink_r211(void) {
    /* REGEL211 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt211;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL211, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL211, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt211 = _cx1;
    }
    return alt211;
}

static uint32_t ink_r212(void) {
    /* REGEL212 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt212;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL212, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL212, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt212 = _cx1;
    }
    return alt212;
}

static uint32_t ink_r213(void) {
    /* REGEL213 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt213;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL213, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL213, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt213 = _cx1;
    }
    return alt213;
}

static uint32_t ink_r214(void) {
    /* REGEL214 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt214;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL214, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL214, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt214 = _cx1;
    }
    return alt214;
}

static uint32_t ink_r215(void) {
    /* REGEL215 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt215;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL215, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL215, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt215 = _cx1;
    }
    return alt215;
}

static uint32_t ink_r216(void) {
    /* REGEL216 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt216;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL216, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL216, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt216 = _cx1;
    }
    return alt216;
}

static uint32_t ink_r217(void) {
    /* REGEL217 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt217;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL217, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL217, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt217 = _cx1;
    }
    return alt217;
}

static uint32_t ink_r218(void) {
    /* REGEL218 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt218;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL218, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL218, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt218 = _cx1;
    }
    return alt218;
}

static uint32_t ink_r219(void) {
    /* REGEL219 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt219;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL219, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL219, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt219 = _cx1;
    }
    return alt219;
}

static uint32_t ink_r220(void) {
    /* REGEL220 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt220;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL220, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL220, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt220 = _cx1;
    }
    return alt220;
}

static uint32_t ink_r221(void) {
    /* REGEL221 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt221;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL221, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL221, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt221 = _cx1;
    }
    return alt221;
}

static uint32_t ink_r222(void) {
    /* REGEL222 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt222;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL222, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL222, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt222 = _cx1;
    }
    return alt222;
}

static uint32_t ink_r223(void) {
    /* REGEL223 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt223;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL223, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL223, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt223 = _cx1;
    }
    return alt223;
}

static uint32_t ink_r224(void) {
    /* REGEL224 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt224;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL224, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL224, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt224 = _cx1;
    }
    return alt224;
}

static uint32_t ink_r225(void) {
    /* REGEL225 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt225;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL225, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL225, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt225 = _cx1;
    }
    return alt225;
}

static uint32_t ink_r226(void) {
    /* REGEL226 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt226;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL226, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL226, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt226 = _cx1;
    }
    return alt226;
}

static uint32_t ink_r227(void) {
    /* REGEL227 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt227;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL227, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL227, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt227 = _cx1;
    }
    return alt227;
}

static uint32_t ink_r228(void) {
    /* REGEL228 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt228;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL228, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL228, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt228 = _cx1;
    }
    return alt228;
}

static uint32_t ink_r229(void) {
    /* REGEL229 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt229;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL229, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL229, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt229 = _cx1;
    }
    return alt229;
}

static uint32_t ink_r230(void) {
    /* REGEL230 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt230;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL230, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL230, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt230 = _cx1;
    }
    return alt230;
}

static uint32_t ink_r231(void) {
    /* REGEL231 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt231;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL231, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL231, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt231 = _cx1;
    }
    return alt231;
}

static uint32_t ink_r232(void) {
    /* REGEL232 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt232;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL232, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL232, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt232 = _cx1;
    }
    return alt232;
}

static uint32_t ink_r233(void) {
    /* REGEL233 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt233;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL233, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL233, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt233 = _cx1;
    }
    return alt233;
}

static uint32_t ink_r234(void) {
    /* REGEL234 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt234;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL234, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL234, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt234 = _cx1;
    }
    return alt234;
}

static uint32_t ink_r235(void) {
    /* REGEL235 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt235;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL235, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL235, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt235 = _cx1;
    }
    return alt235;
}

static uint32_t ink_r236(void) {
    /* REGEL236 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt236;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL236, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL236, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt236 = _cx1;
    }
    return alt236;
}

static uint32_t ink_r237(void) {
    /* REGEL237 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt237;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL237, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL237, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt237 = _cx1;
    }
    return alt237;
}

static uint32_t ink_r238(void) {
    /* REGEL238 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt238;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL238, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL238, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt238 = _cx1;
    }
    return alt238;
}

static uint32_t ink_r239(void) {
    /* REGEL239 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt239;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL239, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL239, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt239 = _cx1;
    }
    return alt239;
}

static uint32_t ink_r240(void) {
    /* REGEL240 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt240;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL240, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL240, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt240 = _cx1;
    }
    return alt240;
}

static uint32_t ink_r241(void) {
    /* REGEL241 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt241;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL241, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL241, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt241 = _cx1;
    }
    return alt241;
}

static uint32_t ink_r242(void) {
    /* REGEL242 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt242;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL242, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL242, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt242 = _cx1;
    }
    return alt242;
}

static uint32_t ink_r243(void) {
    /* REGEL243 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt243;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL243, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL243, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt243 = _cx1;
    }
    return alt243;
}

static uint32_t ink_r244(void) {
    /* REGEL244 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt244;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL244, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL244, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt244 = _cx1;
    }
    return alt244;
}

static uint32_t ink_r245(void) {
    /* REGEL245 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt245;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL245, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL245, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt245 = _cx1;
    }
    return alt245;
}

static uint32_t ink_r246(void) {
    /* REGEL246 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt246;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL246, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL246, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt246 = _cx1;
    }
    return alt246;
}

static uint32_t ink_r247(void) {
    /* REGEL247 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt247;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL247, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL247, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt247 = _cx1;
    }
    return alt247;
}

static uint32_t ink_r248(void) {
    /* REGEL248 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt248;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL248, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL248, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt248 = _cx1;
    }
    return alt248;
}

static uint32_t ink_r249(void) {
    /* REGEL249 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt249;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL249, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL249, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt249 = _cx1;
    }
    return alt249;
}

static uint32_t ink_r250(void) {
    /* REGEL250 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt250;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL250, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL250, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt250 = _cx1;
    }
    return alt250;
}

static uint32_t ink_r251(void) {
    /* REGEL251 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt251;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL251, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL251, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt251 = _cx1;
    }
    return alt251;
}

static uint32_t ink_r252(void) {
    /* REGEL252 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt252;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL252, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL252, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt252 = _cx1;
    }
    return alt252;
}

static uint32_t ink_r253(void) {
    /* REGEL253 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt253;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL253, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL253, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt253 = _cx1;
    }
    return alt253;
}

static uint32_t ink_r254(void) {
    /* REGEL254 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt254;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL254, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL254, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt254 = _cx1;
    }
    return alt254;
}

static uint32_t ink_r255(void) {
    /* REGEL255 exchange update(t) -- a bounded CAS loop, and bounded is
     * the point: SPRACHE.md forbids an unbounded one. The body computes
     * old -> new and is pure, so re-running it on a lost race is free of
     * consequence. `zaehler_streit` is the exit at 64 passes. */
    uint32_t alt255;
    {
        uint32_t _ci1 = 0;
        uint32_t _cx1 = atomic_load_explicit(&REGEL255, memory_order_relaxed);
        for (;;) {
            uint32_t _cn1;
            {
                const uint32_t t = _cx1;
                _cn1 = folge(t); goto _cn1_fertig;
                _cn1_fertig: ;
            }
            if (atomic_compare_exchange_weak_explicit(
                    &REGEL255, &_cx1, _cn1, memory_order_relaxed, memory_order_relaxed)) break;
            if (_ci1 >= (uint32_t)(64)) { zaehler_streit(); }
            _ci1++;
        }
        alt255 = _cx1;
    }
    return alt255;
}

void zaehle_urteil(uint32_t u) {
    if (u == 0) {
        (void)ink_verworfen();
    } else {
        if (u == 1) {
            (void)ink_akzeptiert();
        } else {
            (void)ink_abgewiesen();
        }
    }
    (void)ink_gesamt();
}

void zaehle_regel(uint32_t r) {
    if (r == 0) {
        (void)ink_r000();
    }
    if (r == 1) {
        (void)ink_r001();
    }
    if (r == 2) {
        (void)ink_r002();
    }
    if (r == 3) {
        (void)ink_r003();
    }
    if (r == 4) {
        (void)ink_r004();
    }
    if (r == 5) {
        (void)ink_r005();
    }
    if (r == 6) {
        (void)ink_r006();
    }
    if (r == 7) {
        (void)ink_r007();
    }
    if (r == 8) {
        (void)ink_r008();
    }
    if (r == 9) {
        (void)ink_r009();
    }
    if (r == 10) {
        (void)ink_r010();
    }
    if (r == 11) {
        (void)ink_r011();
    }
    if (r == 12) {
        (void)ink_r012();
    }
    if (r == 13) {
        (void)ink_r013();
    }
    if (r == 14) {
        (void)ink_r014();
    }
    if (r == 15) {
        (void)ink_r015();
    }
    if (r == 16) {
        (void)ink_r016();
    }
    if (r == 17) {
        (void)ink_r017();
    }
    if (r == 18) {
        (void)ink_r018();
    }
    if (r == 19) {
        (void)ink_r019();
    }
    if (r == 20) {
        (void)ink_r020();
    }
    if (r == 21) {
        (void)ink_r021();
    }
    if (r == 22) {
        (void)ink_r022();
    }
    if (r == 23) {
        (void)ink_r023();
    }
    if (r == 24) {
        (void)ink_r024();
    }
    if (r == 25) {
        (void)ink_r025();
    }
    if (r == 26) {
        (void)ink_r026();
    }
    if (r == 27) {
        (void)ink_r027();
    }
    if (r == 28) {
        (void)ink_r028();
    }
    if (r == 29) {
        (void)ink_r029();
    }
    if (r == 30) {
        (void)ink_r030();
    }
    if (r == 31) {
        (void)ink_r031();
    }
    if (r == 32) {
        (void)ink_r032();
    }
    if (r == 33) {
        (void)ink_r033();
    }
    if (r == 34) {
        (void)ink_r034();
    }
    if (r == 35) {
        (void)ink_r035();
    }
    if (r == 36) {
        (void)ink_r036();
    }
    if (r == 37) {
        (void)ink_r037();
    }
    if (r == 38) {
        (void)ink_r038();
    }
    if (r == 39) {
        (void)ink_r039();
    }
    if (r == 40) {
        (void)ink_r040();
    }
    if (r == 41) {
        (void)ink_r041();
    }
    if (r == 42) {
        (void)ink_r042();
    }
    if (r == 43) {
        (void)ink_r043();
    }
    if (r == 44) {
        (void)ink_r044();
    }
    if (r == 45) {
        (void)ink_r045();
    }
    if (r == 46) {
        (void)ink_r046();
    }
    if (r == 47) {
        (void)ink_r047();
    }
    if (r == 48) {
        (void)ink_r048();
    }
    if (r == 49) {
        (void)ink_r049();
    }
    if (r == 50) {
        (void)ink_r050();
    }
    if (r == 51) {
        (void)ink_r051();
    }
    if (r == 52) {
        (void)ink_r052();
    }
    if (r == 53) {
        (void)ink_r053();
    }
    if (r == 54) {
        (void)ink_r054();
    }
    if (r == 55) {
        (void)ink_r055();
    }
    if (r == 56) {
        (void)ink_r056();
    }
    if (r == 57) {
        (void)ink_r057();
    }
    if (r == 58) {
        (void)ink_r058();
    }
    if (r == 59) {
        (void)ink_r059();
    }
    if (r == 60) {
        (void)ink_r060();
    }
    if (r == 61) {
        (void)ink_r061();
    }
    if (r == 62) {
        (void)ink_r062();
    }
    if (r == 63) {
        (void)ink_r063();
    }
    if (r == 64) {
        (void)ink_r064();
    }
    if (r == 65) {
        (void)ink_r065();
    }
    if (r == 66) {
        (void)ink_r066();
    }
    if (r == 67) {
        (void)ink_r067();
    }
    if (r == 68) {
        (void)ink_r068();
    }
    if (r == 69) {
        (void)ink_r069();
    }
    if (r == 70) {
        (void)ink_r070();
    }
    if (r == 71) {
        (void)ink_r071();
    }
    if (r == 72) {
        (void)ink_r072();
    }
    if (r == 73) {
        (void)ink_r073();
    }
    if (r == 74) {
        (void)ink_r074();
    }
    if (r == 75) {
        (void)ink_r075();
    }
    if (r == 76) {
        (void)ink_r076();
    }
    if (r == 77) {
        (void)ink_r077();
    }
    if (r == 78) {
        (void)ink_r078();
    }
    if (r == 79) {
        (void)ink_r079();
    }
    if (r == 80) {
        (void)ink_r080();
    }
    if (r == 81) {
        (void)ink_r081();
    }
    if (r == 82) {
        (void)ink_r082();
    }
    if (r == 83) {
        (void)ink_r083();
    }
    if (r == 84) {
        (void)ink_r084();
    }
    if (r == 85) {
        (void)ink_r085();
    }
    if (r == 86) {
        (void)ink_r086();
    }
    if (r == 87) {
        (void)ink_r087();
    }
    if (r == 88) {
        (void)ink_r088();
    }
    if (r == 89) {
        (void)ink_r089();
    }
    if (r == 90) {
        (void)ink_r090();
    }
    if (r == 91) {
        (void)ink_r091();
    }
    if (r == 92) {
        (void)ink_r092();
    }
    if (r == 93) {
        (void)ink_r093();
    }
    if (r == 94) {
        (void)ink_r094();
    }
    if (r == 95) {
        (void)ink_r095();
    }
    if (r == 96) {
        (void)ink_r096();
    }
    if (r == 97) {
        (void)ink_r097();
    }
    if (r == 98) {
        (void)ink_r098();
    }
    if (r == 99) {
        (void)ink_r099();
    }
    if (r == 100) {
        (void)ink_r100();
    }
    if (r == 101) {
        (void)ink_r101();
    }
    if (r == 102) {
        (void)ink_r102();
    }
    if (r == 103) {
        (void)ink_r103();
    }
    if (r == 104) {
        (void)ink_r104();
    }
    if (r == 105) {
        (void)ink_r105();
    }
    if (r == 106) {
        (void)ink_r106();
    }
    if (r == 107) {
        (void)ink_r107();
    }
    if (r == 108) {
        (void)ink_r108();
    }
    if (r == 109) {
        (void)ink_r109();
    }
    if (r == 110) {
        (void)ink_r110();
    }
    if (r == 111) {
        (void)ink_r111();
    }
    if (r == 112) {
        (void)ink_r112();
    }
    if (r == 113) {
        (void)ink_r113();
    }
    if (r == 114) {
        (void)ink_r114();
    }
    if (r == 115) {
        (void)ink_r115();
    }
    if (r == 116) {
        (void)ink_r116();
    }
    if (r == 117) {
        (void)ink_r117();
    }
    if (r == 118) {
        (void)ink_r118();
    }
    if (r == 119) {
        (void)ink_r119();
    }
    if (r == 120) {
        (void)ink_r120();
    }
    if (r == 121) {
        (void)ink_r121();
    }
    if (r == 122) {
        (void)ink_r122();
    }
    if (r == 123) {
        (void)ink_r123();
    }
    if (r == 124) {
        (void)ink_r124();
    }
    if (r == 125) {
        (void)ink_r125();
    }
    if (r == 126) {
        (void)ink_r126();
    }
    if (r == 127) {
        (void)ink_r127();
    }
    if (r == 128) {
        (void)ink_r128();
    }
    if (r == 129) {
        (void)ink_r129();
    }
    if (r == 130) {
        (void)ink_r130();
    }
    if (r == 131) {
        (void)ink_r131();
    }
    if (r == 132) {
        (void)ink_r132();
    }
    if (r == 133) {
        (void)ink_r133();
    }
    if (r == 134) {
        (void)ink_r134();
    }
    if (r == 135) {
        (void)ink_r135();
    }
    if (r == 136) {
        (void)ink_r136();
    }
    if (r == 137) {
        (void)ink_r137();
    }
    if (r == 138) {
        (void)ink_r138();
    }
    if (r == 139) {
        (void)ink_r139();
    }
    if (r == 140) {
        (void)ink_r140();
    }
    if (r == 141) {
        (void)ink_r141();
    }
    if (r == 142) {
        (void)ink_r142();
    }
    if (r == 143) {
        (void)ink_r143();
    }
    if (r == 144) {
        (void)ink_r144();
    }
    if (r == 145) {
        (void)ink_r145();
    }
    if (r == 146) {
        (void)ink_r146();
    }
    if (r == 147) {
        (void)ink_r147();
    }
    if (r == 148) {
        (void)ink_r148();
    }
    if (r == 149) {
        (void)ink_r149();
    }
    if (r == 150) {
        (void)ink_r150();
    }
    if (r == 151) {
        (void)ink_r151();
    }
    if (r == 152) {
        (void)ink_r152();
    }
    if (r == 153) {
        (void)ink_r153();
    }
    if (r == 154) {
        (void)ink_r154();
    }
    if (r == 155) {
        (void)ink_r155();
    }
    if (r == 156) {
        (void)ink_r156();
    }
    if (r == 157) {
        (void)ink_r157();
    }
    if (r == 158) {
        (void)ink_r158();
    }
    if (r == 159) {
        (void)ink_r159();
    }
    if (r == 160) {
        (void)ink_r160();
    }
    if (r == 161) {
        (void)ink_r161();
    }
    if (r == 162) {
        (void)ink_r162();
    }
    if (r == 163) {
        (void)ink_r163();
    }
    if (r == 164) {
        (void)ink_r164();
    }
    if (r == 165) {
        (void)ink_r165();
    }
    if (r == 166) {
        (void)ink_r166();
    }
    if (r == 167) {
        (void)ink_r167();
    }
    if (r == 168) {
        (void)ink_r168();
    }
    if (r == 169) {
        (void)ink_r169();
    }
    if (r == 170) {
        (void)ink_r170();
    }
    if (r == 171) {
        (void)ink_r171();
    }
    if (r == 172) {
        (void)ink_r172();
    }
    if (r == 173) {
        (void)ink_r173();
    }
    if (r == 174) {
        (void)ink_r174();
    }
    if (r == 175) {
        (void)ink_r175();
    }
    if (r == 176) {
        (void)ink_r176();
    }
    if (r == 177) {
        (void)ink_r177();
    }
    if (r == 178) {
        (void)ink_r178();
    }
    if (r == 179) {
        (void)ink_r179();
    }
    if (r == 180) {
        (void)ink_r180();
    }
    if (r == 181) {
        (void)ink_r181();
    }
    if (r == 182) {
        (void)ink_r182();
    }
    if (r == 183) {
        (void)ink_r183();
    }
    if (r == 184) {
        (void)ink_r184();
    }
    if (r == 185) {
        (void)ink_r185();
    }
    if (r == 186) {
        (void)ink_r186();
    }
    if (r == 187) {
        (void)ink_r187();
    }
    if (r == 188) {
        (void)ink_r188();
    }
    if (r == 189) {
        (void)ink_r189();
    }
    if (r == 190) {
        (void)ink_r190();
    }
    if (r == 191) {
        (void)ink_r191();
    }
    if (r == 192) {
        (void)ink_r192();
    }
    if (r == 193) {
        (void)ink_r193();
    }
    if (r == 194) {
        (void)ink_r194();
    }
    if (r == 195) {
        (void)ink_r195();
    }
    if (r == 196) {
        (void)ink_r196();
    }
    if (r == 197) {
        (void)ink_r197();
    }
    if (r == 198) {
        (void)ink_r198();
    }
    if (r == 199) {
        (void)ink_r199();
    }
    if (r == 200) {
        (void)ink_r200();
    }
    if (r == 201) {
        (void)ink_r201();
    }
    if (r == 202) {
        (void)ink_r202();
    }
    if (r == 203) {
        (void)ink_r203();
    }
    if (r == 204) {
        (void)ink_r204();
    }
    if (r == 205) {
        (void)ink_r205();
    }
    if (r == 206) {
        (void)ink_r206();
    }
    if (r == 207) {
        (void)ink_r207();
    }
    if (r == 208) {
        (void)ink_r208();
    }
    if (r == 209) {
        (void)ink_r209();
    }
    if (r == 210) {
        (void)ink_r210();
    }
    if (r == 211) {
        (void)ink_r211();
    }
    if (r == 212) {
        (void)ink_r212();
    }
    if (r == 213) {
        (void)ink_r213();
    }
    if (r == 214) {
        (void)ink_r214();
    }
    if (r == 215) {
        (void)ink_r215();
    }
    if (r == 216) {
        (void)ink_r216();
    }
    if (r == 217) {
        (void)ink_r217();
    }
    if (r == 218) {
        (void)ink_r218();
    }
    if (r == 219) {
        (void)ink_r219();
    }
    if (r == 220) {
        (void)ink_r220();
    }
    if (r == 221) {
        (void)ink_r221();
    }
    if (r == 222) {
        (void)ink_r222();
    }
    if (r == 223) {
        (void)ink_r223();
    }
    if (r == 224) {
        (void)ink_r224();
    }
    if (r == 225) {
        (void)ink_r225();
    }
    if (r == 226) {
        (void)ink_r226();
    }
    if (r == 227) {
        (void)ink_r227();
    }
    if (r == 228) {
        (void)ink_r228();
    }
    if (r == 229) {
        (void)ink_r229();
    }
    if (r == 230) {
        (void)ink_r230();
    }
    if (r == 231) {
        (void)ink_r231();
    }
    if (r == 232) {
        (void)ink_r232();
    }
    if (r == 233) {
        (void)ink_r233();
    }
    if (r == 234) {
        (void)ink_r234();
    }
    if (r == 235) {
        (void)ink_r235();
    }
    if (r == 236) {
        (void)ink_r236();
    }
    if (r == 237) {
        (void)ink_r237();
    }
    if (r == 238) {
        (void)ink_r238();
    }
    if (r == 239) {
        (void)ink_r239();
    }
    if (r == 240) {
        (void)ink_r240();
    }
    if (r == 241) {
        (void)ink_r241();
    }
    if (r == 242) {
        (void)ink_r242();
    }
    if (r == 243) {
        (void)ink_r243();
    }
    if (r == 244) {
        (void)ink_r244();
    }
    if (r == 245) {
        (void)ink_r245();
    }
    if (r == 246) {
        (void)ink_r246();
    }
    if (r == 247) {
        (void)ink_r247();
    }
    if (r == 248) {
        (void)ink_r248();
    }
    if (r == 249) {
        (void)ink_r249();
    }
    if (r == 250) {
        (void)ink_r250();
    }
    if (r == 251) {
        (void)ink_r251();
    }
    if (r == 252) {
        (void)ink_r252();
    }
    if (r == 253) {
        (void)ink_r253();
    }
    if (r == 254) {
        (void)ink_r254();
    }
    if (r == 255) {
        (void)ink_r255();
    }
    (void)ink_regelsumme();
}

void zaehle_fehl(void) {
    (void)ink_fehlform();
}

void zaehle_voll(void) {
    (void)ink_voll();
}

uint32_t lies_zaehler(uint32_t welcher) {
    if (welcher == 0) {
        uint32_t n0 = atomic_load_explicit(&GESAMT, memory_order_relaxed);
        return n0;
    } else {
        if (welcher == 1) {
            uint32_t n1 = atomic_load_explicit(&VERWORFEN, memory_order_relaxed);
            return n1;
        } else {
            if (welcher == 2) {
                uint32_t n2 = atomic_load_explicit(&AKZEPTIERT, memory_order_relaxed);
                return n2;
            } else {
                if (welcher == 3) {
                    uint32_t n3 = atomic_load_explicit(&ABGEWIESEN, memory_order_relaxed);
                    return n3;
                } else {
                    if (welcher == 4) {
                        uint32_t n4 = atomic_load_explicit(&FEHLFORM, memory_order_relaxed);
                        return n4;
                    } else {
                        if (welcher == 5) {
                            uint32_t n5 = atomic_load_explicit(&VOLL, memory_order_relaxed);
                            return n5;
                        } else {
                            if (welcher == 6) {
                                uint32_t n6 = atomic_load_explicit(&REGELSUMME, memory_order_relaxed);
                                return n6;
                            } else {
                                if (welcher == 7) {
                                    return 0;
                                } else {
                                    if (welcher == 8) {
                                        return 0;
                                    } else {
                                        if (welcher == 9) {
                                            return 0;
                                        } else {
                                            if (welcher == 10) {
                                                return 0;
                                            } else {
                                                if (welcher == 11) {
                                                    return 0;
                                                } else {
                                                    if (welcher == 12) {
                                                        return 0;
                                                    } else {
                                                        if (welcher == 13) {
                                                            return 0;
                                                        } else {
                                                            if (welcher == 14) {
                                                                return 0;
                                                            } else {
                                                                if (welcher == 15) {
                                                                    return 0;
                                                                } else {
                                                                    return 0;
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

uint32_t lies_regel_treffer(uint32_t r) {
    uint32_t erg = 0;
    if (r == 0) {
        uint32_t g000 = atomic_load_explicit(&REGEL000, memory_order_relaxed);
        erg = g000;
    }
    if (r == 1) {
        uint32_t g001 = atomic_load_explicit(&REGEL001, memory_order_relaxed);
        erg = g001;
    }
    if (r == 2) {
        uint32_t g002 = atomic_load_explicit(&REGEL002, memory_order_relaxed);
        erg = g002;
    }
    if (r == 3) {
        uint32_t g003 = atomic_load_explicit(&REGEL003, memory_order_relaxed);
        erg = g003;
    }
    if (r == 4) {
        uint32_t g004 = atomic_load_explicit(&REGEL004, memory_order_relaxed);
        erg = g004;
    }
    if (r == 5) {
        uint32_t g005 = atomic_load_explicit(&REGEL005, memory_order_relaxed);
        erg = g005;
    }
    if (r == 6) {
        uint32_t g006 = atomic_load_explicit(&REGEL006, memory_order_relaxed);
        erg = g006;
    }
    if (r == 7) {
        uint32_t g007 = atomic_load_explicit(&REGEL007, memory_order_relaxed);
        erg = g007;
    }
    if (r == 8) {
        uint32_t g008 = atomic_load_explicit(&REGEL008, memory_order_relaxed);
        erg = g008;
    }
    if (r == 9) {
        uint32_t g009 = atomic_load_explicit(&REGEL009, memory_order_relaxed);
        erg = g009;
    }
    if (r == 10) {
        uint32_t g010 = atomic_load_explicit(&REGEL010, memory_order_relaxed);
        erg = g010;
    }
    if (r == 11) {
        uint32_t g011 = atomic_load_explicit(&REGEL011, memory_order_relaxed);
        erg = g011;
    }
    if (r == 12) {
        uint32_t g012 = atomic_load_explicit(&REGEL012, memory_order_relaxed);
        erg = g012;
    }
    if (r == 13) {
        uint32_t g013 = atomic_load_explicit(&REGEL013, memory_order_relaxed);
        erg = g013;
    }
    if (r == 14) {
        uint32_t g014 = atomic_load_explicit(&REGEL014, memory_order_relaxed);
        erg = g014;
    }
    if (r == 15) {
        uint32_t g015 = atomic_load_explicit(&REGEL015, memory_order_relaxed);
        erg = g015;
    }
    if (r == 16) {
        uint32_t g016 = atomic_load_explicit(&REGEL016, memory_order_relaxed);
        erg = g016;
    }
    if (r == 17) {
        uint32_t g017 = atomic_load_explicit(&REGEL017, memory_order_relaxed);
        erg = g017;
    }
    if (r == 18) {
        uint32_t g018 = atomic_load_explicit(&REGEL018, memory_order_relaxed);
        erg = g018;
    }
    if (r == 19) {
        uint32_t g019 = atomic_load_explicit(&REGEL019, memory_order_relaxed);
        erg = g019;
    }
    if (r == 20) {
        uint32_t g020 = atomic_load_explicit(&REGEL020, memory_order_relaxed);
        erg = g020;
    }
    if (r == 21) {
        uint32_t g021 = atomic_load_explicit(&REGEL021, memory_order_relaxed);
        erg = g021;
    }
    if (r == 22) {
        uint32_t g022 = atomic_load_explicit(&REGEL022, memory_order_relaxed);
        erg = g022;
    }
    if (r == 23) {
        uint32_t g023 = atomic_load_explicit(&REGEL023, memory_order_relaxed);
        erg = g023;
    }
    if (r == 24) {
        uint32_t g024 = atomic_load_explicit(&REGEL024, memory_order_relaxed);
        erg = g024;
    }
    if (r == 25) {
        uint32_t g025 = atomic_load_explicit(&REGEL025, memory_order_relaxed);
        erg = g025;
    }
    if (r == 26) {
        uint32_t g026 = atomic_load_explicit(&REGEL026, memory_order_relaxed);
        erg = g026;
    }
    if (r == 27) {
        uint32_t g027 = atomic_load_explicit(&REGEL027, memory_order_relaxed);
        erg = g027;
    }
    if (r == 28) {
        uint32_t g028 = atomic_load_explicit(&REGEL028, memory_order_relaxed);
        erg = g028;
    }
    if (r == 29) {
        uint32_t g029 = atomic_load_explicit(&REGEL029, memory_order_relaxed);
        erg = g029;
    }
    if (r == 30) {
        uint32_t g030 = atomic_load_explicit(&REGEL030, memory_order_relaxed);
        erg = g030;
    }
    if (r == 31) {
        uint32_t g031 = atomic_load_explicit(&REGEL031, memory_order_relaxed);
        erg = g031;
    }
    if (r == 32) {
        uint32_t g032 = atomic_load_explicit(&REGEL032, memory_order_relaxed);
        erg = g032;
    }
    if (r == 33) {
        uint32_t g033 = atomic_load_explicit(&REGEL033, memory_order_relaxed);
        erg = g033;
    }
    if (r == 34) {
        uint32_t g034 = atomic_load_explicit(&REGEL034, memory_order_relaxed);
        erg = g034;
    }
    if (r == 35) {
        uint32_t g035 = atomic_load_explicit(&REGEL035, memory_order_relaxed);
        erg = g035;
    }
    if (r == 36) {
        uint32_t g036 = atomic_load_explicit(&REGEL036, memory_order_relaxed);
        erg = g036;
    }
    if (r == 37) {
        uint32_t g037 = atomic_load_explicit(&REGEL037, memory_order_relaxed);
        erg = g037;
    }
    if (r == 38) {
        uint32_t g038 = atomic_load_explicit(&REGEL038, memory_order_relaxed);
        erg = g038;
    }
    if (r == 39) {
        uint32_t g039 = atomic_load_explicit(&REGEL039, memory_order_relaxed);
        erg = g039;
    }
    if (r == 40) {
        uint32_t g040 = atomic_load_explicit(&REGEL040, memory_order_relaxed);
        erg = g040;
    }
    if (r == 41) {
        uint32_t g041 = atomic_load_explicit(&REGEL041, memory_order_relaxed);
        erg = g041;
    }
    if (r == 42) {
        uint32_t g042 = atomic_load_explicit(&REGEL042, memory_order_relaxed);
        erg = g042;
    }
    if (r == 43) {
        uint32_t g043 = atomic_load_explicit(&REGEL043, memory_order_relaxed);
        erg = g043;
    }
    if (r == 44) {
        uint32_t g044 = atomic_load_explicit(&REGEL044, memory_order_relaxed);
        erg = g044;
    }
    if (r == 45) {
        uint32_t g045 = atomic_load_explicit(&REGEL045, memory_order_relaxed);
        erg = g045;
    }
    if (r == 46) {
        uint32_t g046 = atomic_load_explicit(&REGEL046, memory_order_relaxed);
        erg = g046;
    }
    if (r == 47) {
        uint32_t g047 = atomic_load_explicit(&REGEL047, memory_order_relaxed);
        erg = g047;
    }
    if (r == 48) {
        uint32_t g048 = atomic_load_explicit(&REGEL048, memory_order_relaxed);
        erg = g048;
    }
    if (r == 49) {
        uint32_t g049 = atomic_load_explicit(&REGEL049, memory_order_relaxed);
        erg = g049;
    }
    if (r == 50) {
        uint32_t g050 = atomic_load_explicit(&REGEL050, memory_order_relaxed);
        erg = g050;
    }
    if (r == 51) {
        uint32_t g051 = atomic_load_explicit(&REGEL051, memory_order_relaxed);
        erg = g051;
    }
    if (r == 52) {
        uint32_t g052 = atomic_load_explicit(&REGEL052, memory_order_relaxed);
        erg = g052;
    }
    if (r == 53) {
        uint32_t g053 = atomic_load_explicit(&REGEL053, memory_order_relaxed);
        erg = g053;
    }
    if (r == 54) {
        uint32_t g054 = atomic_load_explicit(&REGEL054, memory_order_relaxed);
        erg = g054;
    }
    if (r == 55) {
        uint32_t g055 = atomic_load_explicit(&REGEL055, memory_order_relaxed);
        erg = g055;
    }
    if (r == 56) {
        uint32_t g056 = atomic_load_explicit(&REGEL056, memory_order_relaxed);
        erg = g056;
    }
    if (r == 57) {
        uint32_t g057 = atomic_load_explicit(&REGEL057, memory_order_relaxed);
        erg = g057;
    }
    if (r == 58) {
        uint32_t g058 = atomic_load_explicit(&REGEL058, memory_order_relaxed);
        erg = g058;
    }
    if (r == 59) {
        uint32_t g059 = atomic_load_explicit(&REGEL059, memory_order_relaxed);
        erg = g059;
    }
    if (r == 60) {
        uint32_t g060 = atomic_load_explicit(&REGEL060, memory_order_relaxed);
        erg = g060;
    }
    if (r == 61) {
        uint32_t g061 = atomic_load_explicit(&REGEL061, memory_order_relaxed);
        erg = g061;
    }
    if (r == 62) {
        uint32_t g062 = atomic_load_explicit(&REGEL062, memory_order_relaxed);
        erg = g062;
    }
    if (r == 63) {
        uint32_t g063 = atomic_load_explicit(&REGEL063, memory_order_relaxed);
        erg = g063;
    }
    if (r == 64) {
        uint32_t g064 = atomic_load_explicit(&REGEL064, memory_order_relaxed);
        erg = g064;
    }
    if (r == 65) {
        uint32_t g065 = atomic_load_explicit(&REGEL065, memory_order_relaxed);
        erg = g065;
    }
    if (r == 66) {
        uint32_t g066 = atomic_load_explicit(&REGEL066, memory_order_relaxed);
        erg = g066;
    }
    if (r == 67) {
        uint32_t g067 = atomic_load_explicit(&REGEL067, memory_order_relaxed);
        erg = g067;
    }
    if (r == 68) {
        uint32_t g068 = atomic_load_explicit(&REGEL068, memory_order_relaxed);
        erg = g068;
    }
    if (r == 69) {
        uint32_t g069 = atomic_load_explicit(&REGEL069, memory_order_relaxed);
        erg = g069;
    }
    if (r == 70) {
        uint32_t g070 = atomic_load_explicit(&REGEL070, memory_order_relaxed);
        erg = g070;
    }
    if (r == 71) {
        uint32_t g071 = atomic_load_explicit(&REGEL071, memory_order_relaxed);
        erg = g071;
    }
    if (r == 72) {
        uint32_t g072 = atomic_load_explicit(&REGEL072, memory_order_relaxed);
        erg = g072;
    }
    if (r == 73) {
        uint32_t g073 = atomic_load_explicit(&REGEL073, memory_order_relaxed);
        erg = g073;
    }
    if (r == 74) {
        uint32_t g074 = atomic_load_explicit(&REGEL074, memory_order_relaxed);
        erg = g074;
    }
    if (r == 75) {
        uint32_t g075 = atomic_load_explicit(&REGEL075, memory_order_relaxed);
        erg = g075;
    }
    if (r == 76) {
        uint32_t g076 = atomic_load_explicit(&REGEL076, memory_order_relaxed);
        erg = g076;
    }
    if (r == 77) {
        uint32_t g077 = atomic_load_explicit(&REGEL077, memory_order_relaxed);
        erg = g077;
    }
    if (r == 78) {
        uint32_t g078 = atomic_load_explicit(&REGEL078, memory_order_relaxed);
        erg = g078;
    }
    if (r == 79) {
        uint32_t g079 = atomic_load_explicit(&REGEL079, memory_order_relaxed);
        erg = g079;
    }
    if (r == 80) {
        uint32_t g080 = atomic_load_explicit(&REGEL080, memory_order_relaxed);
        erg = g080;
    }
    if (r == 81) {
        uint32_t g081 = atomic_load_explicit(&REGEL081, memory_order_relaxed);
        erg = g081;
    }
    if (r == 82) {
        uint32_t g082 = atomic_load_explicit(&REGEL082, memory_order_relaxed);
        erg = g082;
    }
    if (r == 83) {
        uint32_t g083 = atomic_load_explicit(&REGEL083, memory_order_relaxed);
        erg = g083;
    }
    if (r == 84) {
        uint32_t g084 = atomic_load_explicit(&REGEL084, memory_order_relaxed);
        erg = g084;
    }
    if (r == 85) {
        uint32_t g085 = atomic_load_explicit(&REGEL085, memory_order_relaxed);
        erg = g085;
    }
    if (r == 86) {
        uint32_t g086 = atomic_load_explicit(&REGEL086, memory_order_relaxed);
        erg = g086;
    }
    if (r == 87) {
        uint32_t g087 = atomic_load_explicit(&REGEL087, memory_order_relaxed);
        erg = g087;
    }
    if (r == 88) {
        uint32_t g088 = atomic_load_explicit(&REGEL088, memory_order_relaxed);
        erg = g088;
    }
    if (r == 89) {
        uint32_t g089 = atomic_load_explicit(&REGEL089, memory_order_relaxed);
        erg = g089;
    }
    if (r == 90) {
        uint32_t g090 = atomic_load_explicit(&REGEL090, memory_order_relaxed);
        erg = g090;
    }
    if (r == 91) {
        uint32_t g091 = atomic_load_explicit(&REGEL091, memory_order_relaxed);
        erg = g091;
    }
    if (r == 92) {
        uint32_t g092 = atomic_load_explicit(&REGEL092, memory_order_relaxed);
        erg = g092;
    }
    if (r == 93) {
        uint32_t g093 = atomic_load_explicit(&REGEL093, memory_order_relaxed);
        erg = g093;
    }
    if (r == 94) {
        uint32_t g094 = atomic_load_explicit(&REGEL094, memory_order_relaxed);
        erg = g094;
    }
    if (r == 95) {
        uint32_t g095 = atomic_load_explicit(&REGEL095, memory_order_relaxed);
        erg = g095;
    }
    if (r == 96) {
        uint32_t g096 = atomic_load_explicit(&REGEL096, memory_order_relaxed);
        erg = g096;
    }
    if (r == 97) {
        uint32_t g097 = atomic_load_explicit(&REGEL097, memory_order_relaxed);
        erg = g097;
    }
    if (r == 98) {
        uint32_t g098 = atomic_load_explicit(&REGEL098, memory_order_relaxed);
        erg = g098;
    }
    if (r == 99) {
        uint32_t g099 = atomic_load_explicit(&REGEL099, memory_order_relaxed);
        erg = g099;
    }
    if (r == 100) {
        uint32_t g100 = atomic_load_explicit(&REGEL100, memory_order_relaxed);
        erg = g100;
    }
    if (r == 101) {
        uint32_t g101 = atomic_load_explicit(&REGEL101, memory_order_relaxed);
        erg = g101;
    }
    if (r == 102) {
        uint32_t g102 = atomic_load_explicit(&REGEL102, memory_order_relaxed);
        erg = g102;
    }
    if (r == 103) {
        uint32_t g103 = atomic_load_explicit(&REGEL103, memory_order_relaxed);
        erg = g103;
    }
    if (r == 104) {
        uint32_t g104 = atomic_load_explicit(&REGEL104, memory_order_relaxed);
        erg = g104;
    }
    if (r == 105) {
        uint32_t g105 = atomic_load_explicit(&REGEL105, memory_order_relaxed);
        erg = g105;
    }
    if (r == 106) {
        uint32_t g106 = atomic_load_explicit(&REGEL106, memory_order_relaxed);
        erg = g106;
    }
    if (r == 107) {
        uint32_t g107 = atomic_load_explicit(&REGEL107, memory_order_relaxed);
        erg = g107;
    }
    if (r == 108) {
        uint32_t g108 = atomic_load_explicit(&REGEL108, memory_order_relaxed);
        erg = g108;
    }
    if (r == 109) {
        uint32_t g109 = atomic_load_explicit(&REGEL109, memory_order_relaxed);
        erg = g109;
    }
    if (r == 110) {
        uint32_t g110 = atomic_load_explicit(&REGEL110, memory_order_relaxed);
        erg = g110;
    }
    if (r == 111) {
        uint32_t g111 = atomic_load_explicit(&REGEL111, memory_order_relaxed);
        erg = g111;
    }
    if (r == 112) {
        uint32_t g112 = atomic_load_explicit(&REGEL112, memory_order_relaxed);
        erg = g112;
    }
    if (r == 113) {
        uint32_t g113 = atomic_load_explicit(&REGEL113, memory_order_relaxed);
        erg = g113;
    }
    if (r == 114) {
        uint32_t g114 = atomic_load_explicit(&REGEL114, memory_order_relaxed);
        erg = g114;
    }
    if (r == 115) {
        uint32_t g115 = atomic_load_explicit(&REGEL115, memory_order_relaxed);
        erg = g115;
    }
    if (r == 116) {
        uint32_t g116 = atomic_load_explicit(&REGEL116, memory_order_relaxed);
        erg = g116;
    }
    if (r == 117) {
        uint32_t g117 = atomic_load_explicit(&REGEL117, memory_order_relaxed);
        erg = g117;
    }
    if (r == 118) {
        uint32_t g118 = atomic_load_explicit(&REGEL118, memory_order_relaxed);
        erg = g118;
    }
    if (r == 119) {
        uint32_t g119 = atomic_load_explicit(&REGEL119, memory_order_relaxed);
        erg = g119;
    }
    if (r == 120) {
        uint32_t g120 = atomic_load_explicit(&REGEL120, memory_order_relaxed);
        erg = g120;
    }
    if (r == 121) {
        uint32_t g121 = atomic_load_explicit(&REGEL121, memory_order_relaxed);
        erg = g121;
    }
    if (r == 122) {
        uint32_t g122 = atomic_load_explicit(&REGEL122, memory_order_relaxed);
        erg = g122;
    }
    if (r == 123) {
        uint32_t g123 = atomic_load_explicit(&REGEL123, memory_order_relaxed);
        erg = g123;
    }
    if (r == 124) {
        uint32_t g124 = atomic_load_explicit(&REGEL124, memory_order_relaxed);
        erg = g124;
    }
    if (r == 125) {
        uint32_t g125 = atomic_load_explicit(&REGEL125, memory_order_relaxed);
        erg = g125;
    }
    if (r == 126) {
        uint32_t g126 = atomic_load_explicit(&REGEL126, memory_order_relaxed);
        erg = g126;
    }
    if (r == 127) {
        uint32_t g127 = atomic_load_explicit(&REGEL127, memory_order_relaxed);
        erg = g127;
    }
    if (r == 128) {
        uint32_t g128 = atomic_load_explicit(&REGEL128, memory_order_relaxed);
        erg = g128;
    }
    if (r == 129) {
        uint32_t g129 = atomic_load_explicit(&REGEL129, memory_order_relaxed);
        erg = g129;
    }
    if (r == 130) {
        uint32_t g130 = atomic_load_explicit(&REGEL130, memory_order_relaxed);
        erg = g130;
    }
    if (r == 131) {
        uint32_t g131 = atomic_load_explicit(&REGEL131, memory_order_relaxed);
        erg = g131;
    }
    if (r == 132) {
        uint32_t g132 = atomic_load_explicit(&REGEL132, memory_order_relaxed);
        erg = g132;
    }
    if (r == 133) {
        uint32_t g133 = atomic_load_explicit(&REGEL133, memory_order_relaxed);
        erg = g133;
    }
    if (r == 134) {
        uint32_t g134 = atomic_load_explicit(&REGEL134, memory_order_relaxed);
        erg = g134;
    }
    if (r == 135) {
        uint32_t g135 = atomic_load_explicit(&REGEL135, memory_order_relaxed);
        erg = g135;
    }
    if (r == 136) {
        uint32_t g136 = atomic_load_explicit(&REGEL136, memory_order_relaxed);
        erg = g136;
    }
    if (r == 137) {
        uint32_t g137 = atomic_load_explicit(&REGEL137, memory_order_relaxed);
        erg = g137;
    }
    if (r == 138) {
        uint32_t g138 = atomic_load_explicit(&REGEL138, memory_order_relaxed);
        erg = g138;
    }
    if (r == 139) {
        uint32_t g139 = atomic_load_explicit(&REGEL139, memory_order_relaxed);
        erg = g139;
    }
    if (r == 140) {
        uint32_t g140 = atomic_load_explicit(&REGEL140, memory_order_relaxed);
        erg = g140;
    }
    if (r == 141) {
        uint32_t g141 = atomic_load_explicit(&REGEL141, memory_order_relaxed);
        erg = g141;
    }
    if (r == 142) {
        uint32_t g142 = atomic_load_explicit(&REGEL142, memory_order_relaxed);
        erg = g142;
    }
    if (r == 143) {
        uint32_t g143 = atomic_load_explicit(&REGEL143, memory_order_relaxed);
        erg = g143;
    }
    if (r == 144) {
        uint32_t g144 = atomic_load_explicit(&REGEL144, memory_order_relaxed);
        erg = g144;
    }
    if (r == 145) {
        uint32_t g145 = atomic_load_explicit(&REGEL145, memory_order_relaxed);
        erg = g145;
    }
    if (r == 146) {
        uint32_t g146 = atomic_load_explicit(&REGEL146, memory_order_relaxed);
        erg = g146;
    }
    if (r == 147) {
        uint32_t g147 = atomic_load_explicit(&REGEL147, memory_order_relaxed);
        erg = g147;
    }
    if (r == 148) {
        uint32_t g148 = atomic_load_explicit(&REGEL148, memory_order_relaxed);
        erg = g148;
    }
    if (r == 149) {
        uint32_t g149 = atomic_load_explicit(&REGEL149, memory_order_relaxed);
        erg = g149;
    }
    if (r == 150) {
        uint32_t g150 = atomic_load_explicit(&REGEL150, memory_order_relaxed);
        erg = g150;
    }
    if (r == 151) {
        uint32_t g151 = atomic_load_explicit(&REGEL151, memory_order_relaxed);
        erg = g151;
    }
    if (r == 152) {
        uint32_t g152 = atomic_load_explicit(&REGEL152, memory_order_relaxed);
        erg = g152;
    }
    if (r == 153) {
        uint32_t g153 = atomic_load_explicit(&REGEL153, memory_order_relaxed);
        erg = g153;
    }
    if (r == 154) {
        uint32_t g154 = atomic_load_explicit(&REGEL154, memory_order_relaxed);
        erg = g154;
    }
    if (r == 155) {
        uint32_t g155 = atomic_load_explicit(&REGEL155, memory_order_relaxed);
        erg = g155;
    }
    if (r == 156) {
        uint32_t g156 = atomic_load_explicit(&REGEL156, memory_order_relaxed);
        erg = g156;
    }
    if (r == 157) {
        uint32_t g157 = atomic_load_explicit(&REGEL157, memory_order_relaxed);
        erg = g157;
    }
    if (r == 158) {
        uint32_t g158 = atomic_load_explicit(&REGEL158, memory_order_relaxed);
        erg = g158;
    }
    if (r == 159) {
        uint32_t g159 = atomic_load_explicit(&REGEL159, memory_order_relaxed);
        erg = g159;
    }
    if (r == 160) {
        uint32_t g160 = atomic_load_explicit(&REGEL160, memory_order_relaxed);
        erg = g160;
    }
    if (r == 161) {
        uint32_t g161 = atomic_load_explicit(&REGEL161, memory_order_relaxed);
        erg = g161;
    }
    if (r == 162) {
        uint32_t g162 = atomic_load_explicit(&REGEL162, memory_order_relaxed);
        erg = g162;
    }
    if (r == 163) {
        uint32_t g163 = atomic_load_explicit(&REGEL163, memory_order_relaxed);
        erg = g163;
    }
    if (r == 164) {
        uint32_t g164 = atomic_load_explicit(&REGEL164, memory_order_relaxed);
        erg = g164;
    }
    if (r == 165) {
        uint32_t g165 = atomic_load_explicit(&REGEL165, memory_order_relaxed);
        erg = g165;
    }
    if (r == 166) {
        uint32_t g166 = atomic_load_explicit(&REGEL166, memory_order_relaxed);
        erg = g166;
    }
    if (r == 167) {
        uint32_t g167 = atomic_load_explicit(&REGEL167, memory_order_relaxed);
        erg = g167;
    }
    if (r == 168) {
        uint32_t g168 = atomic_load_explicit(&REGEL168, memory_order_relaxed);
        erg = g168;
    }
    if (r == 169) {
        uint32_t g169 = atomic_load_explicit(&REGEL169, memory_order_relaxed);
        erg = g169;
    }
    if (r == 170) {
        uint32_t g170 = atomic_load_explicit(&REGEL170, memory_order_relaxed);
        erg = g170;
    }
    if (r == 171) {
        uint32_t g171 = atomic_load_explicit(&REGEL171, memory_order_relaxed);
        erg = g171;
    }
    if (r == 172) {
        uint32_t g172 = atomic_load_explicit(&REGEL172, memory_order_relaxed);
        erg = g172;
    }
    if (r == 173) {
        uint32_t g173 = atomic_load_explicit(&REGEL173, memory_order_relaxed);
        erg = g173;
    }
    if (r == 174) {
        uint32_t g174 = atomic_load_explicit(&REGEL174, memory_order_relaxed);
        erg = g174;
    }
    if (r == 175) {
        uint32_t g175 = atomic_load_explicit(&REGEL175, memory_order_relaxed);
        erg = g175;
    }
    if (r == 176) {
        uint32_t g176 = atomic_load_explicit(&REGEL176, memory_order_relaxed);
        erg = g176;
    }
    if (r == 177) {
        uint32_t g177 = atomic_load_explicit(&REGEL177, memory_order_relaxed);
        erg = g177;
    }
    if (r == 178) {
        uint32_t g178 = atomic_load_explicit(&REGEL178, memory_order_relaxed);
        erg = g178;
    }
    if (r == 179) {
        uint32_t g179 = atomic_load_explicit(&REGEL179, memory_order_relaxed);
        erg = g179;
    }
    if (r == 180) {
        uint32_t g180 = atomic_load_explicit(&REGEL180, memory_order_relaxed);
        erg = g180;
    }
    if (r == 181) {
        uint32_t g181 = atomic_load_explicit(&REGEL181, memory_order_relaxed);
        erg = g181;
    }
    if (r == 182) {
        uint32_t g182 = atomic_load_explicit(&REGEL182, memory_order_relaxed);
        erg = g182;
    }
    if (r == 183) {
        uint32_t g183 = atomic_load_explicit(&REGEL183, memory_order_relaxed);
        erg = g183;
    }
    if (r == 184) {
        uint32_t g184 = atomic_load_explicit(&REGEL184, memory_order_relaxed);
        erg = g184;
    }
    if (r == 185) {
        uint32_t g185 = atomic_load_explicit(&REGEL185, memory_order_relaxed);
        erg = g185;
    }
    if (r == 186) {
        uint32_t g186 = atomic_load_explicit(&REGEL186, memory_order_relaxed);
        erg = g186;
    }
    if (r == 187) {
        uint32_t g187 = atomic_load_explicit(&REGEL187, memory_order_relaxed);
        erg = g187;
    }
    if (r == 188) {
        uint32_t g188 = atomic_load_explicit(&REGEL188, memory_order_relaxed);
        erg = g188;
    }
    if (r == 189) {
        uint32_t g189 = atomic_load_explicit(&REGEL189, memory_order_relaxed);
        erg = g189;
    }
    if (r == 190) {
        uint32_t g190 = atomic_load_explicit(&REGEL190, memory_order_relaxed);
        erg = g190;
    }
    if (r == 191) {
        uint32_t g191 = atomic_load_explicit(&REGEL191, memory_order_relaxed);
        erg = g191;
    }
    if (r == 192) {
        uint32_t g192 = atomic_load_explicit(&REGEL192, memory_order_relaxed);
        erg = g192;
    }
    if (r == 193) {
        uint32_t g193 = atomic_load_explicit(&REGEL193, memory_order_relaxed);
        erg = g193;
    }
    if (r == 194) {
        uint32_t g194 = atomic_load_explicit(&REGEL194, memory_order_relaxed);
        erg = g194;
    }
    if (r == 195) {
        uint32_t g195 = atomic_load_explicit(&REGEL195, memory_order_relaxed);
        erg = g195;
    }
    if (r == 196) {
        uint32_t g196 = atomic_load_explicit(&REGEL196, memory_order_relaxed);
        erg = g196;
    }
    if (r == 197) {
        uint32_t g197 = atomic_load_explicit(&REGEL197, memory_order_relaxed);
        erg = g197;
    }
    if (r == 198) {
        uint32_t g198 = atomic_load_explicit(&REGEL198, memory_order_relaxed);
        erg = g198;
    }
    if (r == 199) {
        uint32_t g199 = atomic_load_explicit(&REGEL199, memory_order_relaxed);
        erg = g199;
    }
    if (r == 200) {
        uint32_t g200 = atomic_load_explicit(&REGEL200, memory_order_relaxed);
        erg = g200;
    }
    if (r == 201) {
        uint32_t g201 = atomic_load_explicit(&REGEL201, memory_order_relaxed);
        erg = g201;
    }
    if (r == 202) {
        uint32_t g202 = atomic_load_explicit(&REGEL202, memory_order_relaxed);
        erg = g202;
    }
    if (r == 203) {
        uint32_t g203 = atomic_load_explicit(&REGEL203, memory_order_relaxed);
        erg = g203;
    }
    if (r == 204) {
        uint32_t g204 = atomic_load_explicit(&REGEL204, memory_order_relaxed);
        erg = g204;
    }
    if (r == 205) {
        uint32_t g205 = atomic_load_explicit(&REGEL205, memory_order_relaxed);
        erg = g205;
    }
    if (r == 206) {
        uint32_t g206 = atomic_load_explicit(&REGEL206, memory_order_relaxed);
        erg = g206;
    }
    if (r == 207) {
        uint32_t g207 = atomic_load_explicit(&REGEL207, memory_order_relaxed);
        erg = g207;
    }
    if (r == 208) {
        uint32_t g208 = atomic_load_explicit(&REGEL208, memory_order_relaxed);
        erg = g208;
    }
    if (r == 209) {
        uint32_t g209 = atomic_load_explicit(&REGEL209, memory_order_relaxed);
        erg = g209;
    }
    if (r == 210) {
        uint32_t g210 = atomic_load_explicit(&REGEL210, memory_order_relaxed);
        erg = g210;
    }
    if (r == 211) {
        uint32_t g211 = atomic_load_explicit(&REGEL211, memory_order_relaxed);
        erg = g211;
    }
    if (r == 212) {
        uint32_t g212 = atomic_load_explicit(&REGEL212, memory_order_relaxed);
        erg = g212;
    }
    if (r == 213) {
        uint32_t g213 = atomic_load_explicit(&REGEL213, memory_order_relaxed);
        erg = g213;
    }
    if (r == 214) {
        uint32_t g214 = atomic_load_explicit(&REGEL214, memory_order_relaxed);
        erg = g214;
    }
    if (r == 215) {
        uint32_t g215 = atomic_load_explicit(&REGEL215, memory_order_relaxed);
        erg = g215;
    }
    if (r == 216) {
        uint32_t g216 = atomic_load_explicit(&REGEL216, memory_order_relaxed);
        erg = g216;
    }
    if (r == 217) {
        uint32_t g217 = atomic_load_explicit(&REGEL217, memory_order_relaxed);
        erg = g217;
    }
    if (r == 218) {
        uint32_t g218 = atomic_load_explicit(&REGEL218, memory_order_relaxed);
        erg = g218;
    }
    if (r == 219) {
        uint32_t g219 = atomic_load_explicit(&REGEL219, memory_order_relaxed);
        erg = g219;
    }
    if (r == 220) {
        uint32_t g220 = atomic_load_explicit(&REGEL220, memory_order_relaxed);
        erg = g220;
    }
    if (r == 221) {
        uint32_t g221 = atomic_load_explicit(&REGEL221, memory_order_relaxed);
        erg = g221;
    }
    if (r == 222) {
        uint32_t g222 = atomic_load_explicit(&REGEL222, memory_order_relaxed);
        erg = g222;
    }
    if (r == 223) {
        uint32_t g223 = atomic_load_explicit(&REGEL223, memory_order_relaxed);
        erg = g223;
    }
    if (r == 224) {
        uint32_t g224 = atomic_load_explicit(&REGEL224, memory_order_relaxed);
        erg = g224;
    }
    if (r == 225) {
        uint32_t g225 = atomic_load_explicit(&REGEL225, memory_order_relaxed);
        erg = g225;
    }
    if (r == 226) {
        uint32_t g226 = atomic_load_explicit(&REGEL226, memory_order_relaxed);
        erg = g226;
    }
    if (r == 227) {
        uint32_t g227 = atomic_load_explicit(&REGEL227, memory_order_relaxed);
        erg = g227;
    }
    if (r == 228) {
        uint32_t g228 = atomic_load_explicit(&REGEL228, memory_order_relaxed);
        erg = g228;
    }
    if (r == 229) {
        uint32_t g229 = atomic_load_explicit(&REGEL229, memory_order_relaxed);
        erg = g229;
    }
    if (r == 230) {
        uint32_t g230 = atomic_load_explicit(&REGEL230, memory_order_relaxed);
        erg = g230;
    }
    if (r == 231) {
        uint32_t g231 = atomic_load_explicit(&REGEL231, memory_order_relaxed);
        erg = g231;
    }
    if (r == 232) {
        uint32_t g232 = atomic_load_explicit(&REGEL232, memory_order_relaxed);
        erg = g232;
    }
    if (r == 233) {
        uint32_t g233 = atomic_load_explicit(&REGEL233, memory_order_relaxed);
        erg = g233;
    }
    if (r == 234) {
        uint32_t g234 = atomic_load_explicit(&REGEL234, memory_order_relaxed);
        erg = g234;
    }
    if (r == 235) {
        uint32_t g235 = atomic_load_explicit(&REGEL235, memory_order_relaxed);
        erg = g235;
    }
    if (r == 236) {
        uint32_t g236 = atomic_load_explicit(&REGEL236, memory_order_relaxed);
        erg = g236;
    }
    if (r == 237) {
        uint32_t g237 = atomic_load_explicit(&REGEL237, memory_order_relaxed);
        erg = g237;
    }
    if (r == 238) {
        uint32_t g238 = atomic_load_explicit(&REGEL238, memory_order_relaxed);
        erg = g238;
    }
    if (r == 239) {
        uint32_t g239 = atomic_load_explicit(&REGEL239, memory_order_relaxed);
        erg = g239;
    }
    if (r == 240) {
        uint32_t g240 = atomic_load_explicit(&REGEL240, memory_order_relaxed);
        erg = g240;
    }
    if (r == 241) {
        uint32_t g241 = atomic_load_explicit(&REGEL241, memory_order_relaxed);
        erg = g241;
    }
    if (r == 242) {
        uint32_t g242 = atomic_load_explicit(&REGEL242, memory_order_relaxed);
        erg = g242;
    }
    if (r == 243) {
        uint32_t g243 = atomic_load_explicit(&REGEL243, memory_order_relaxed);
        erg = g243;
    }
    if (r == 244) {
        uint32_t g244 = atomic_load_explicit(&REGEL244, memory_order_relaxed);
        erg = g244;
    }
    if (r == 245) {
        uint32_t g245 = atomic_load_explicit(&REGEL245, memory_order_relaxed);
        erg = g245;
    }
    if (r == 246) {
        uint32_t g246 = atomic_load_explicit(&REGEL246, memory_order_relaxed);
        erg = g246;
    }
    if (r == 247) {
        uint32_t g247 = atomic_load_explicit(&REGEL247, memory_order_relaxed);
        erg = g247;
    }
    if (r == 248) {
        uint32_t g248 = atomic_load_explicit(&REGEL248, memory_order_relaxed);
        erg = g248;
    }
    if (r == 249) {
        uint32_t g249 = atomic_load_explicit(&REGEL249, memory_order_relaxed);
        erg = g249;
    }
    if (r == 250) {
        uint32_t g250 = atomic_load_explicit(&REGEL250, memory_order_relaxed);
        erg = g250;
    }
    if (r == 251) {
        uint32_t g251 = atomic_load_explicit(&REGEL251, memory_order_relaxed);
        erg = g251;
    }
    if (r == 252) {
        uint32_t g252 = atomic_load_explicit(&REGEL252, memory_order_relaxed);
        erg = g252;
    }
    if (r == 253) {
        uint32_t g253 = atomic_load_explicit(&REGEL253, memory_order_relaxed);
        erg = g253;
    }
    if (r == 254) {
        uint32_t g254 = atomic_load_explicit(&REGEL254, memory_order_relaxed);
        erg = g254;
    }
    if (r == 255) {
        uint32_t g255 = atomic_load_explicit(&REGEL255, memory_order_relaxed);
        erg = g255;
    }
    return erg;
}
