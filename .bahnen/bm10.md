YOUR MODULE: `gab/lauf.gab` — and the one thing that is still missing for this firewall to receive a single packet.

**Measured on the merged tree, 2026-09-16, with `strace` on the linked binary:**

```
socket(AF_NETLINK, SOCK_RAW, NETLINK_NETFILTER) = 3
mmap(NULL, 65536, PROT_READ|PROT_WRITE, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0) = 0x7d87...
recvfrom(3, <blocks forever>
```

**No `bind`, and no configuration message.** The queue is never bound, so the kernel has no reason to hand this program anything, and the worker waits for something that will never come. `gab/netzverbindung.gab` already builds every message this needs — the bind command, the copy mode, the parse of an incoming message, the verdict — and `gab/entscheidung.gab` already decides. The loop simply never calls them.

Close it, in `gab/lauf.gab` (and mirrors only — do not change another module's logic):

1. **`bind`** the netlink socket to the process's own address (the syscall is declared in `gab/systemrufe.gab`).
2. **Send the configuration**: bind queue 0 to this socket, then set copy mode to packet with a copy range that fits the `Paket` table, through `netzverbindung`'s builders.
3. **In the loop**: `recvfrom` → parse the message (`netzverbindung`) → fill the `Paket` table → call `entscheide(laenge, faden)` → build the verdict message with the packet id → `sendto`. `EINTR`/`EAGAIN` are not failures; `ENOBUFS` is counted and reported.
4. **Then MEASURE IT, and this is the deliverable**: you have no root in this clone, so write the exact procedure — the `nft` rule, the namespace, the traffic — and run everything you can without privileges (the message builders against a hand-made netlink message, the parse, the verdict bytes). Say precisely which step needs root and what it would show.

**The stub** (`VSPERRE_nimm`/`_gib` and the four diverging exits) stays scratch and out of the tree, as lane bm9 left it; say in the report what the shipped runtime would have to define instead.

Write `messung/BERICHT-bm10.md`: the wiring, the checker's words, the emitted C at the send and receive sites, what ran, what needs root, and every refusal verbatim.
